# BinSifter (Winnow) - TODO

_Last updated: 2026-08-19 (Winnow promoted to beta + README overhaul; Defender-exclusion Settings feature added to both variants; installer scaffolding for both variants added - see installer/README.md's caveats; 38-unparseable-files investigation resolved, not a bug - see Correctness / trust section; AI-assisted triage feasibility prototype tested and concluded NOT viable on real hardware (GPU/display lockup) - see AI-assisted triage exploration section; AI export (Markdown + JSON) feature shipped in both variants as the safer follow-up - see AI export section; multi-vendor antivirus detection shipped in both variants, and a real Rowan runspace-scoping bug in the Defender exclusion button found and fixed along the way - see Multi-vendor antivirus detection section; first real installer test on FLARE VM found and fixed a scan-crashing Rowan bug and a capa version-check timeout - see First real installer test section; first real Winnow installer test found and fixed a startup-crashing eager-import bug, plus a likely explanation for a Defender false-positive flag - see First real Winnow installer test section; second real Rowan installer test, against real case work, found and fixed three more real bugs (draft YARA rule generation silently skipped, AI export crashing every use, Speakeasy quick-launch documentation gap) - see Second real Rowan installer test section; same-day re-test then found two more bugs hiding behind those fixes - see Re-test after first-round fixes section; second real Winnow installer test found and fixed an admin-install startup crash (Program Files not writable by a non-elevated launch) and a missing multiprocessing.freeze_support() call that spawned a full GUI window per worker mid-scan - see Second real Winnow installer test section; cross-machine Rowan installer test found and fixed 8 uses of .NET 5+-only hashing APIs (HashData/ToHexString) that crashed on a PowerShell 7.0 (older, .NET Core 3.1) machine while working fine on a newer PowerShell 7.x host - see Second real Rowan installer test, cross-machine section; third real Winnow installer test - the first where the window actually rendered - found and fixed a PyInstaller-6.0-layout-change bug that silently blanked every logo/title-bar icon, and a scan-stalling bug where a single file hanging in any untimed pipeline stage could block an entire scan forever - see Third real Winnow installer test section; **Stop button fix - found and fixed the root cause (should_stop() was dead code after submission finished) plus two more real bugs surfaced while testing the fix itself (discarding already-completed results, and cancelling fresh-but-not-stuck files before real worker spawn-up had time to finish) - see Winnow Stop button not actually stopping a running scan section; found and fixed a real bug behind the footer's "not installed" YARA/Capa/SSDEEP status (PyInstaller doesn't bundle package version metadata by default - see Winnow footer status bar section); investigated the NSRL cache appearing to rebuild every run - mechanism looks sound, most likely explanation is the source file's mtime changing between sessions, but found and fixed a real latent gap (blank ReportDirectory landing the cache in an unstable CWD-relative location) along the way - see NSRL cache section; found and fixed a real Rowan bug where a genuine Add-Type compile failure would have been completely silent until hours later, because the runspace it runs in never inherits $ErrorActionPreference = 'Stop' - see Rowan silently failing to start a scan on FLARE VM section; found and fixed the actual root cause of a real 4+ hour Winnow scan - a third-party (signify) caching gap made every catalog-verification check re-parse its whole catalog from scratch on every single file, averaging 61.8s/file (92.5% of all CPU time) against a real 5730-file CatRoot corpus - see Winnow authenticode stage section; found and fixed real DPI-scaling bugs in both variants behind a "dashboard/button text skewed and doesn't fit" report - Rowan had no DPI-awareness declared at all, Winnow's fixed-pixel label geometry didn't grow to match its own font at fractional scale factors - see Dashboard/nav-button text skewed section; a real side-by-side log comparison confirmed the FLARE VM slowness is the same catalog-caching bug fixed the day before, not FLARE-VM-specific, and found + fixed a real correctness gap where CatalogDirectory pointed at the bare CatRoot folder silently found zero catalogs (real ones live one level deeper) - see Fourth real Winnow/Rowan test round section; found the REAL reason the previous day's Rowan DPI fix had no visible effect - AutoScaleMode.Dpi does nothing without also setting AutoScaleDimensions, which a hand-built (no Designer) form never had - see Rowan DPI fix from 2026-08-17 didn't actually work section; that AutoScaleDimensions fix ALSO had no effect on real hardware - a standalone diagnostic script run directly on the affected host PC proved AutoScaleDimensions was already silently reading the live runtime DPI before any of our code used it, so PerformAutoScale() always computed a 1.0 ratio; fixed by bypassing it entirely with a manual Control.Scale() call using a DeviceDpi/96.0 ratio, applied to all three hand-built forms - see Rowan DPI fix from 2026-08-18 ALSO didn't work section**)_

## Conventions

- **New features get implemented across all active variants (Rowan + Winnow) by default, in the same pass, unless told otherwise.** Stated directly 2026-08-07 after archive/compressed-file support initially shipped Winnow-only. Bug fixes/small changes were already being replicated this way earlier in the project (see the dashboard "Signed" tile/color-pass entries below, both done in Rowan and Winnow together) - this makes it the explicit default for anything new too, not just fixes. Ingot (Rust) stays parked/excluded until it's actually started.

## First real installer test (FLARE VM, Rowan) - 2026-08-09

The first real end-to-end test of a built installer, on a real Windows analysis VM (FLARE VM) rather than this dev sandbox.

- [x] **Installer itself: clean pass.** `BinSifter-Rowan_setup.exe`, built via the GitHub Actions workflow and downloaded from the release, installed with no issues. PowerShell 7 wasn't preinstalled on FLARE VM - installed manually via `winget`, one reboot, then the desktop shortcut launched Rowan with no errors. Confirms the installer scaffolding and the workflow's build output are both sound in practice, not just in theory - this was the first time either had been exercised outside this dev sandbox.
- [x] **Working from a VM-shared folder was slow enough to abandon.** First scan attempt had tools/NSRL file/samples all on a VM share rather than local disk and had to be stopped - moving everything to the VM's own desktop before the second attempt resolved it. Not a BinSifter bug, just a real-world setup note worth remembering for future test rounds: point `SrcDir`/`ToolsDir`/`NsrlPath` at local disk, not a shared/mounted folder, for anything beyond a tiny smoke test.
- [x] **REAL BUG FOUND AND FIXED: archive-expansion scan dispatch crashed immediately with "Cannot find an overload for "new" and the argument count: "1"." (`BinSifter-Rowan_v1.3.0-beta.1.ps1`, scan-engine dispatcher).** Second scan (19 files - 7 archived Malware Bazaar samples, all password-protected, extracted successfully via the shared-password prompt) got all the way through enumeration and archive extraction (`Archive expansion pass 2: 12 more file(s) extracted...`, `Target queue populated: 19 files.`) and then died on the very next line, before a single file was dispatched to a worker - Scan Queue showed all 19 files stuck at `Queued`/0% forever ("Scan finished. 0 / 19 files completed."). Root-caused from the real log (`Rowan_logs.txt`): `$orderedPaths` starts out as a genuinely .NET-typed `List[string]` (`EnumerationResult.Files`), but the archive-expansion code above rebuilds it via `$orderedPaths = @($orderedPaths) + @($expansion.ExtractedFiles)` (and again for password-resolved files) - PowerShell's `@()` array-subexpression operator flattens that into a loosely-typed `System.Object[]`, even though every element is still genuinely a string. The next line, `[System.Collections.Generic.Queue[string]]::new($orderedPaths)`, has to resolve between `Queue`'s `(int capacity)` and `(IEnumerable[string] collection)` constructors, and PowerShell's `::new()` overload binder can fail to disambiguate those two against an untyped `Object[]` argument - "Cannot find an overload for 'new'" is PowerShell's own wording for exactly that binder failure, thrown before the constructor itself ever runs. This is the **only** 1-argument `::new()` call on a generic .NET collection anywhere in the file (confirmed via search - every other generic-collection construction elsewhere uses either zero arguments or an `int` capacity), and archive expansion had never previously run end-to-end against a live Rowan scan reaching this exact line - so this had been sitting latent since archive support shipped, never triggered by any prior confirmed real-Winnow-only or synthetic Rowan test. **Fixed** by casting explicitly to `[string[]]` right at the point of construction (`[System.Collections.Generic.Queue[string]]::new([string[]]$orderedPaths)`) - a genuine `String[]` unambiguously satisfies `IEnumerable[string]`, no binder guesswork left. Bracket/brace/paren balance re-verified on just the diff's added/removed lines (both fully balanced, 0 errors) via the same quote/comment-aware scanner used for every other Rowan-only change this project can't run `pwsh` to check directly. **Not yet re-tested against a real scan** - needs a third FLARE VM run to confirm the fix actually resolves it, since this dev sandbox still has no `pwsh` to verify directly.
- [x] **RESOLVED: "Capa: version unavailable" was a probe-timeout issue, not a broken capa.** Asked for `capa.exe --version` run directly in a terminal on the same FLARE VM - answered immediately and correctly (`capa.exe 9.3.1`), confirming capa.exe itself is fine; the disconnect was specifically between how `Get-VersionText` invokes it and a plain terminal run. capa's official Windows release is a PyInstaller onefile executable, which self-extracts its whole bundled Python runtime into a fresh `%TEMP%` folder on every launch (not cached between runs) - real, documented PyInstaller behavior, not a guess - making it a fundamentally heavier launch than `yara64.exe`/`ssdeep.exe` (small native binaries, near-instant startup). `Get-VersionText` used one flat 3-second timeout shared across all three tools, which wasn't well-suited to capa's launch profile, especially on a slower-disk analysis VM. **Fixed** by bumping the timeout to 10 seconds - this probe only ever runs once at startup or right after a Settings save, never a hot path, so a longer worst-case wait costs nothing real, and it still fails gracefully to `'version unavailable'` if a tool is genuinely hung. **Not yet re-confirmed against a real BinSifter run** - needs the next FLARE VM test to verify the status bar now shows a real capa version instead of "version unavailable."

## First real Winnow installer test - 2026-08-09

Same day as the Rowan FLARE VM test above, but this one was on both a host system and FLARE VM, and it's Winnow-specific throughout - Rowan shells out to a separate `speakeasy.exe`-equivalent process for its own emulation quick-launch, so it never had this exposure.

- [x] **REAL BUG FOUND AND FIXED: the whole app crashed at startup, on BOTH machines, with an ImportError chain ending in unicorn's "ERROR: fail to load the dynamic library."** Traceback ran `__main__.py` -> `main_window.py` -> `results.py` -> `core/speakeasy_scan.py` -> `speakeasy/__init__.py` -> ... -> `speakeasy/engines/unicorn_eng.py` -> `import unicorn` -> unicorn's own native-DLL loader. Root cause: `results.py` does `from binsifter.core import ai_export, speakeasy_scan` at its own module level (so the GUI can build the Speakeasy quick-launch menu item), and `speakeasy_scan.py` used to do a plain top-level `import speakeasy` - `speakeasy`'s own `__init__.py` eagerly imports its ENTIRE Windows-emulation stack down to `unicorn` at import time, which loads `unicorn`'s native DLL via `ctypes` immediately, not lazily. That means ANY failure to load that one native DLL took down the entire application before the main window ever appeared, for every user - even someone who never intends to touch Speakeasy emulation at all. Leading explanation for why the DLL itself failed to load: `speakeasy-emulator` hard-pins `unicorn==1.0.2` (see `pyproject.toml`'s dependency comment), an old release whose prebuilt Windows wheel is known in the wider community to need the Microsoft Visual C++ Redistributable (x64) present on the target machine - plausible on both a fresh-ish host and a FLARE VM, though which exact machine was missing what couldn't be confirmed from this Linux dev sandbox. **Fixed with two layers, since the exact root cause couldn't be nailed down for certain without a real Windows repro:**
  1. **`binsifter/core/speakeasy_scan.py`: made the import itself resilient**, same graceful-degradation philosophy `emulate_file()` already used for a target file speakeasy can't handle. The module-level `import speakeasy` is now wrapped in try/except; a failure is stored (`_IMPORT_ERROR`) instead of propagating, and `_DEFAULT_CONFIG_PATH` degrades to `""` rather than crashing on `speakeasy.__file__`. `emulate_file()` checks that stored failure first and returns a normal `SpeakeasyResult` with an actionable `.error` message (names the Visual C++ Redistributable as the likely fix) instead of ever letting the original exception reach `results.py`'s import and crash the app. Every other BinSifter feature (YARA, capa, ssdeep, hashing, Authenticode, the rest of the Results grid) has zero dependency on unicorn/speakeasy and is unaffected either way - only the Speakeasy emulation action itself degrades, with a clear message, instead of the whole app dying. Covered by 3 new tests in `tests/test_speakeasy_scan.py` (this Linux sandbox's real speakeasy/unicorn install works fine - no Windows DLL to fail here - so the real failure mode is simulated by monkeypatching the module's post-failure state directly, not by forcing a real ImportError through a working library).
  2. **`installer/winnow.spec`: added explicit `collect_all("unicorn")`**, wired into `Analysis()`'s `binaries`/`datas`/`hiddenimports` alongside the existing `mscerts`/`speakeasy` collections. Nothing previously explicitly collected unicorn's own native binaries - PyInstaller's automatic community hook (via `pyinstaller-hooks-contrib`, a transitive dependency of `pyinstaller` itself) should normally catch this, but there was no way to confirm that from this Linux sandbox against a real Windows build, and this costs nothing if the automatic path already had it covered.
  3. **README's Requirements section now names the Visual C++ Redistributable (x64) as a Winnow prerequisite** on Windows, with a link and a note that only Speakeasy emulation is affected if it's missing.
  Full test suite re-run: 205 passed (adding 3 new tests to the existing `tests/test_speakeasy_scan.py` - a first attempt at this accidentally overwrote that file's 10 pre-existing tests instead of extending it, caught before committing and merged back in, all 13 tests in that file now pass together), same 3 pre-existing unrelated failures as every prior run this session. **Not yet re-tested against a real installer build** - needs both machines re-tested against a freshly-built installer to confirm the app now starts cleanly either way (with or without the Redistributable installed).
- [x] **Asked directly: is Microsoft Defender's "Trojan:Win32/Malgent" flag on `BinSifter-Winnow.exe` related to Speakeasy?** Very likely yes, though this can't be fixed from BinSifter's own code the way the crash above could. `Trojan:Win32/Malgent` is Defender's generic ML/heuristic bucket for "structurally or behaviorally suspicious, not a specifically identified family" - not a manually-confirmed detection. Bundling `unicorn` (a full CPU emulator, capable of executing arbitrary code in an emulated environment) and `speakeasy` (a Windows API emulation framework literally built for running malware-like code paths) inside a PyInstaller-built executable is a well-known, common heuristic trigger for exactly this class of generic AV flag - other real malware-analysis tools built the same way (Python + PyInstaller + an embedded emulator/hooking library) hit this regularly, it isn't unique to BinSifter. `winnow.spec` already disables UPX compression specifically to avoid stacking a second well-known false-positive trigger on top (see that spec's own comment) - this is the same category of problem, just not something a packaging flag can turn off, since the emulation capability itself is what's being flagged, not just the packer shape. **Not something to chase further in code** - the standard real-world path for this is submitting the built executable to Microsoft's own file-submission portal (submit.microsoft.com) for a false-positive review once a build is otherwise stable, which is a normal, expected step for legitimate tools in this category, not a BinSifter-specific workaround. Once the startup-crash fix above lands, BinSifter's own existing Defender-exclusion Settings button (see the Quick-launch/Defender sections elsewhere in this file) is also available as a local workaround for an individual analyst's own machine, worth pointing out since it couldn't even be reached before this fix (the app crashed before Settings was ever reachable).

## Second real Rowan installer test (real case work) - 2026-08-10

Installer itself confirmed still working cleanly (only manual step needed was copying `7z.exe` into the tools directory, same as the documented archive-support prerequisite). A real 652-file case scan ran to completion; three issues surfaced from the logs and a right-click pass over Results afterward.

- [x] **REAL BUG FOUND AND FIXED: draft YARA rule generation silently skipped every run - `Measure-Object -Property @{Expression=...}` isn't valid syntax for that cmdlet.** Log showed: `Draft YARA rule generation skipped due to error: Cannot bind parameter 'Property'. Cannot convert the "System.Collections.Hashtable" value of type "System.Collections.Hashtable" to type "Microsoft.PowerShell.Commands.PSPropertyExpression".` Root cause confirmed against PowerShell's own documented behavior (not guessed): `Select-Object`/`Sort-Object`/`Group-Object`/`Format-Table` all accept the `@{Name=...; Expression={...}}` calculated-property hashtable idiom, but `Measure-Object`'s `-Property` parameter is typed `PSPropertyExpression[]`, not `[object[]]`, and does **not** accept that hashtable form at all - only a bare scriptblock. The size-range computation for each SSDEEP-cluster draft rule (`$minSize`/`$maxSize`, used to build the `filesize >= X and filesize <= Y` condition) used the hashtable form, so every single cluster's rule generation threw, was caught by the surrounding `try`/`catch`, and logged as skipped - this had never worked, on any run, since the feature shipped (the same reason the 132 real `generated_rules/*.yar` files already in `Reports/` all fall back to the "no common strings found" skeleton condition with no filesize bound: the per-cluster size range was never successfully computed even when common strings *were* found). **Fixed** in `BinSifter-Rowan_v1.3.0-beta.1.ps1`: replaced the two separate hashtable-based `Measure-Object` calls with one bare-scriptblock call requesting both `-Minimum` and `-Maximum` at once (`Measure-Object -Property { (Get-Item -LiteralPath $_.Path -ErrorAction SilentlyContinue).Length } -Minimum -Maximum`) - also cheaper than before (one `Get-Item` pass per member instead of two). Bracket/brace/paren balance re-verified via the same quote/comment-aware scanner used for every other Rowan-only change this dev sandbox can't run `pwsh` to check directly (net delta across added/removed lines: 0 for every bracket type). **Not yet re-run against a real scan** - needs a real case run to confirm rules now generate with a real filesize condition instead of skipping.
- [x] **REAL BUG FOUND AND FIXED: "Export for AI analysis" crashed on every single use - `Exception calling "Contains" with "1" argument(s): "Value cannot be null. (Parameter 'key')"`.** Traced to `ConvertTo-AiExportMarkdown`'s hash-summary line: `foreach ($pair in @(, @('MD5', 'MD5'), @('SHA1', 'SHA1'), @('ssdeep', 'SSDEEP')))`. This file uses a real, working idiom elsewhere for building an array of 2-element row-arrays across *multiple lines* (e.g. `-Rows @( , @('Status', ...) , @('Signer', ...) )`, one `,`-prefixed row per line) - each row there is its own newline-separated statement, and the leading `,` is needed to stop `@()`'s pipeline-style statement-output collection from auto-unrolling that row's 2 elements into 2 separate items. The hash-summary line copied that same leading-comma habit onto a **single-line, already comma-joined list** instead - a fundamentally different construct, since there's no per-statement pipeline unroll there for the comma to cancel out. The result: only the *first* item (`@('MD5', 'MD5')`) got an extra, uncancelled array wrapper (`@(@('MD5','MD5'))`), while the other two items were unaffected - so on the loop's first iteration, `$pair` was a 1-element array, `$pair[1]` silently returned `$null` (PowerShell doesn't throw on a simple out-of-range array read), and `$data.Contains($null)` (an `OrderedDictionary`) threw exactly the observed exception. Deterministic, not data-dependent - this fires on every call, on the very first loop iteration, regardless of which file is selected, matching the real report exactly. **Fixed** by dropping the stray leading comma (`@(@('MD5', 'MD5'), @('SHA1', 'SHA1'), @('ssdeep', 'SSDEEP'))`) - correct for a single-line comma-joined list, where `@()` just needs to wrap the list, not protect each element from a pipeline unroll that doesn't happen here. Confirmed via a targeted search (`@(,\s*@\(` pattern) that this specific single-line-with-leading-comma antipattern appears exactly once in the file - the multi-line `-Rows @( , @(...) ... )` calls elsewhere (Signature/YARA/capa/ssdeep-imphash/Other sections) use the correct per-line idiom and are unaffected, confirmed by the same reasoning that explains why they've never thrown. Bracket/brace/paren balance re-verified the same way as the item above (net delta: 0). **Not yet re-run against a real scan** - needs confirmation the export now completes and produces a correct Markdown hash-summary line.
- [x] **INVESTIGATED: Speakeasy grayed out in the Results right-click menu - not a bug, a documentation gap.** Same "check before guessing" discipline as the earlier Sigcheck investigation: `speakeasyItem.Enabled` is only true once `$Config.SpeakeasyExe` resolves to a real file named `speakeasy.exe` under the configured tools directory - working exactly as designed, and grayed out is the correct, honest state when that file isn't there. The real gap is that, unlike Sigcheck/Ghidra/yara64/capa/ssdeep/7z (each a standalone binary you can just download and drop into the tools folder), **Speakeasy has no such standalone release** - it's the Mandiant/Google `speakeasy-emulator` Python package (`pip install speakeasy-emulator`), and the `speakeasy.exe` this feature needs is the console-script launcher stub pip generates inside that Python environment's `Scripts\` folder, not something to search Sysinternals-style download pages for. The Settings/Help text and the click-time "not configured" message both described it exactly like every other portable-binary tool, which would send anyone looking for a direct download on a dead end. **Fixed the guidance, not the logic** (the enable/disable check itself is correct and needs no code change): both the Help page's "speakeasy.exe" bullet and the click-handler's not-found message now spell out the actual path - install `speakeasy-emulator` via pip, then copy `speakeasy.exe` out of that environment's `Scripts` folder into the tools directory, same as any other tool once it's there. **Not yet confirmed end-to-end** - worth a real pass once `speakeasy.exe` is copied in per the new guidance, to confirm the menu item enables and a real emulation run succeeds (the underlying `Invoke-CapturedTool`/JSON-summary path itself wasn't touched this pass, so it's presumed still correct from its original implementation, not re-verified here).

## Re-test after first-round fixes (same day, 2026-08-10)

Fixed `.ps1` was dropped straight into the installed copy (no reinstall needed - see the installer-doesn't-compile-anything note elsewhere) and re-run against the same real case. Both remaining issues below are a direct consequence of the two try/catch-wrapped code paths above having *never executed past their first bug* in any prior test - fixing bug #1 in each let execution reach a second bug that had been sitting there the whole time, unreachable and untested, same shape as the archive-dispatch/capa-timeout pattern from the FLARE VM round.

- [x] **REAL BUG #2 FOUND AND FIXED: draft YARA rule generation now failed differently - `The term 'if' is not recognized as a name of a cmdlet, function, script file, or executable program.`** Once the `Measure-Object` fix let `$minSize`/`$maxSize` compute successfully, execution reached the cluster's condition-string-building lines for the first time ever: `$cond = "uint16(0) == 0x5A4D and " + (if ($sizeCondition) { "$sizeCondition and " } else { '' }) + "(3 of them)"` (and the equivalent `$fallbackCond` line just below it). Confirmed against documented PowerShell grammar (not guessed): bare `(...)` parens are the "parenthetical expression" operator and only accept a single **pipeline**, not a **statement** - `if`/`else` is a statement, not an expression, so wrapping it in bare `(...)` and using the result as an operand of `+` made the parser try to invoke `if` itself as an external command name. `$(...)` (the full subexpression operator) is the construct that actually supports statements like `if`/`for`/`while` inline - `(...)` does not, despite `$x = if (...) {...} else {...}` working fine as a plain top-level assignment (that's a different code path - PowerShell captures a whole statement's output on direct assignment, which isn't the same as embedding it as a sub-expression inside a larger expression). **Fixed** by precomputing the conditional piece into its own variable via a normal top-level `if`/`else` assignment first (`$sizePart = if (...) {...} else {...}`), then concatenating that plain variable - sidesteps the bare-parens restriction entirely rather than switching to `$(...)`. Searched the rest of the file for the same `+ (if (...) {...} else {...})` antipattern (used as an operand inside a larger expression, not a `$(if...)`-wrapped inline value or a plain top-level assignment) - every other `(if (...) {...} else {...})` in the file is correctly `$(if ...)`-prefixed (11 occurrences, e.g. the Scan Queue grid's Yes/No cell values, `Add-Log $(if ...)`), so this was the only instance of the bug. Bracket balance re-verified the same way as every other change this pass (net delta: 0 across the whole diff). **Not yet re-run against a real scan.**
- [x] **REAL BUG (self-inflicted) FOUND AND FIXED: "Export for AI analysis" now failed with `You cannot call a method on a null-valued expression.`** This one was introduced by the *first* round's own fix, not a pre-existing bug: removing the leading comma from the hash-summary `foreach` loop was done as a targeted string replacement that (by mistake) also dropped the line right above it, `$hashBits = [System.Collections.Generic.List[string]]::new()` - so `$hashBits` was left referencing nothing, and `$hashBits.Add(...)` on the loop's first successful match called `.Add()` on `$null`. Static read-through of every other line in `Get-AiExportCompactFields`/`ConvertTo-AiExportMarkdown`/`ConvertTo-AiExportJson`/`Export-FileForAiAnalysis` (checked against every `BinSifter.FileRecord` field that can legitimately be `$null` for a real file) turned up nothing else unguarded before this was found - genuinely just a dropped line from the prior edit, confirmed by diffing against what the function looked like before that edit. **Fixed** by restoring the `$hashBits` initialization line. Also added stage-labeled `try`/`catch` around `Export-FileForAiAnalysis`'s five steps (prepping the output dir, computing the filename, building the Markdown, writing the Markdown, building the JSON, writing the JSON) so that if there's a *third*, still-undiscovered bug in here, the next failure names the exact stage instead of a bare top-level message - this dev sandbox still has no `pwsh` to step through it directly, so this is a deliberate concession to that limitation rather than a claim the function is now fully verified. **Not yet re-run against a real scan.**

## Second real Rowan installer test, cross-machine (host + FLARE VM) - 2026-08-13

Host machine: installed, ran a real scan against actual casework, completed with no issues. Same installer, installed on a FLARE VM (Windows 10 Pro): installed fine, but the scan errored immediately with `Method invocation failed because [System.Security.Cryptography.SHA256] does not contain a method named 'HashData'.`

- [x] **REAL BUG FOUND AND FIXED: `::HashData()`/`[Convert]::ToHexString()` are .NET 5+-only APIs - PowerShell 7.0 (still on .NET Core 3.1) doesn't have either, and a `winget install`-provisioned FLARE VM landed on 7.0 while the host machine already had a newer PowerShell 7.x.** PowerShell 7 is self-contained (bundles its own .NET runtime per release: 7.0→.NET Core 3.1, 7.1→.NET 5, 7.2→.NET 6, and so on) - which exact .NET version a machine gets depends entirely on which PowerShell 7.x release actually got installed, not the underlying Windows version. `SHA1.HashData()`/`SHA256.HashData()` (one-shot static hash shortcuts) and `Convert.ToHexString()` (byte-array-to-hex-string) were both introduced in .NET 5.0 - code using either silently requires PowerShell 7.1+, a constraint that was never stated anywhere (README says "PowerShell 7+") and never hit in this dev sandbox (no `pwsh` here to test with at all) or on the host machine (already had a newer PowerShell 7.x from some earlier point). Found **8 total call sites** across the file, not just the one in the error message - the main per-file SHA-1/MD5 hash loop (every file, every scan) uses `[Convert]::ToHexString()` twice; `Get-ArchiveDestDir` (archive expansion) and two duplicated NSRL-cache-path-hash blocks (the real scan's pre-scan setup, and the Settings page's "reload NSRL cache" preview button) each use one `::HashData()` + one `ToHexString()` pair - fixing only the one in the error message would have hit the identical error again on the very first file hashed, the same "fix bug #1, immediately hit bug #2" pattern this project has hit before with FLARE VM testing, so all 8 were fixed together in one pass rather than one at a time. **Fixed** by replacing every `::HashData(bytes)` with the older, universally-compatible `::Create()` + `.ComputeHash(bytes)` instance pattern (existed since .NET Framework 1.1), and every `[Convert]::ToHexString(bytes)` with `[System.BitConverter]::ToString(bytes).Replace('-', '')` (same uppercase-hex output, no casing change downstream) - both existed on every .NET version PowerShell 7 has ever shipped, not just the newest ones. Bracket/brace/paren balance re-verified on just the diff's added/removed lines via the same quote/comment-aware scanner used for every other Rowan-only change this dev sandbox can't run `pwsh` to check directly (net delta: 0 across all three bracket types). **Not yet re-tested against a real scan on the FLARE VM** - needs a fresh installer build and a real re-run there to confirm the scan now completes.

## Second real Winnow installer test - 2026-08-13

Fresh installers built via the "Run workflow" manual trigger (see the Actions page - a plain commit push doesn't build anything, only a tag push or a manual dispatch does) and installed on the host system.

- [x] **REAL BUG FOUND AND FIXED: Winnow crashed at startup with an unhandled `PermissionError: [WinError 5] Access is denied: 'C:\\Program Files\\BinSifter Winnow\\Reports'`.** Traceback: `__main__.py` -> `main_window.py:__init__` -> `config.py:build_default_config()` -> `pathlib.py:mkdir`. Root cause: `Winnow.iss` deliberately offers a choice between a per-user install (default, lands under `%LOCALAPPDATA%\Programs`, always writable) and an all-users/admin install (lands under `C:\Program Files`, requires elevation to write to) - this install went down the admin path, and `build_default_config()`/`save_settings_cache()` both unconditionally assumed the install directory (`get_binsifter_root()`) was writable, trying to `mkdir` Reports/Attack/Blocklist and write the settings-cache JSON directly under it on every startup with no fallback. A non-elevated launch (which is how GUI apps normally run, even after an elevated install) can never write to Program Files, so this crashed on literally every launch of an admin-mode install, not just occasionally. **Fixed** by adding `get_binsifter_data_root()` in `config.py`: it still starts from `get_binsifter_root()`, but actually probes for real write access (creates and deletes a small probe file, rather than trusting permission bits, which don't reliably predict actual NTFS/UAC write access) and falls back to `%LOCALAPPDATA%\BinSifter Winnow\` - always writable, no elevation needed - if that probe fails. `build_default_config()` and `save_settings_cache()` now both go through this instead of `get_binsifter_root()` directly; `get_binsifter_root()` itself is untouched and still used as-is for the read-only logo-loading paths in `main_window.py`/`about.py`, which don't need write access and shouldn't change behavior. Covered by 6 new tests in the new `tests/test_config.py` (writable-root passthrough, fallback when the root is locked down, fallback when `LOCALAPPDATA` itself is unset, and a save/reload round trip through the fallback path) - full suite re-run: 211 passed, same 3 pre-existing unrelated failures as every prior run this session. **Not yet re-tested against a real admin-mode install** - needs a real Windows re-test (ideally against a fresh install using both the per-user and the all-users choice) to confirm the app now starts cleanly either way.
- [ ] **Still open: Microsoft Defender flagged Winnow again on this same test.** No new detail beyond what was already investigated on 2026-08-09 (see the First real Winnow installer test section above) - still presumed the same generic heuristic false-positive triggered by bundling `unicorn`/`speakeasy`'s emulation capability inside a PyInstaller build, not a new or different flag. Not re-chased further this pass since no new information (detection name, screenshot) came with this report; the existing guidance (submit to Microsoft's file-submission portal once a build is otherwise stable, or use BinSifter's own Defender-exclusion Settings button locally) still stands.
- [x] **REAL BUG FOUND AND FIXED: starting a scan on the installed build opened up to 16 extra full BinSifter-Winnow.exe GUI windows instead of running background workers - scan never completed.** Reported directly after the Program-Files crash fix above: settings configured fine, Defender exclusion added, scan started, NSRL caching ran for ~20 minutes as expected, and then - right as the logs reported the worker pool deploying ("16 additional workers deployed") - 16 new application windows opened on screen. Root cause: Windows has no `fork()`, so `multiprocessing` (used throughout `engine.py`/`capa_scan.py`/`subprocess_timeout.py`, all via `multiprocessing.get_context("spawn")`) has to relaunch a fresh interpreter per worker and have it import the main module. For a normal, unfrozen script that relaunch runs `python.exe -c <bootstrap>`, which never touches this project's own `if __name__ == "__main__":` block at all - so this had always worked fine in the dev sandbox and under `pip install -e .`. Once frozen by PyInstaller, though, there's no separate `python.exe` - the built exe IS the only executable - so `multiprocessing.spawn` relaunches that same exe with a hidden sentinel flag instead, and it's `multiprocessing.freeze_support()`'s specific job to recognize that sentinel in the child and run straight to the worker function instead of falling through to the rest of the script. Nothing in this codebase ever called it - `winnow.spec`'s existing `"multiprocessing.popen_spawn_win32"` hiddenimport only makes sure the module ships in the build, it doesn't call `freeze_support()` for you, a separate, required, one-line step per Python's own multiprocessing docs. Without it, every spawned worker just re-ran the entire frozen entry script from scratch, `QApplication` and `MainWindow` included - one brand-new GUI window per worker, up to the full worker count. **Fixed** by adding `multiprocessing.freeze_support()` as the first statement inside `binsifter/gui/__main__.py`'s `if __name__ == "__main__":` block, before `QApplication` is ever created (also added to `binsifter/cli.py`'s own entry point defensively, even though only the GUI is frozen into today's installer - `scan_directory()` spawns the identical workers either way, so the headless entry point would hit the same bug the moment it was ever frozen on its own). Not independently unit-testable from this dev sandbox - `freeze_support()`'s actual effect only exists under a real frozen-on-Windows spawn re-exec, which can't be simulated here - full suite re-run to confirm no regressions instead: 211 passed, same 3 pre-existing unrelated failures. **Not yet re-tested against a real scan** - needs a fresh installer build and a real scan run to confirm the worker pool now runs in the background with no extra windows.

## Third real Winnow installer test - 2026-08-14

First test where Winnow's window actually rendered on a real Windows machine - every earlier real-Windows test crashed at or before `MainWindow.__init__` (the eager unicorn import crash, then the Program-Files write-permission crash), so this is also the first time anyone could see what the window actually looked like, or watch a scan run to completion. Two real bugs found, both apparently present since Winnow's very first installer build and simply never observed until now.

- [x] **REAL BUG FOUND AND FIXED: every bundled logo (sidebar, About page) rendered blank, on both a host machine and a FLARE VM, no error anywhere - and the window's title-bar icon was never set at all.** Root-caused via direct reading, not guessing: `installer/winnow.spec`'s `own_datas` bundles the two logo PNGs with a `"."` destination, which lands directly next to the built exe under PyInstaller's OLDER onedir layout - but neither `build_winnow.ps1` nor the release workflow pinned a PyInstaller version (`pip install pyinstaller`, unconstrained, in both places), so every real build already gets whatever's newest on PyPI. PyInstaller 6.0 changed the onedir default to nest bundled datas one level deeper, under an auto-generated `_internal\` subdirectory, instead of flat next to the exe - `get_binsifter_root() / filename` silently stopped finding either PNG, and the calling code's `if logo_path.is_file():` guard (written to skip gracefully rather than error) made this fail completely silently, with nothing in the logs to point at. Separately, the title-bar icon was never a bug at all, just a gap: `winnow.spec`'s `EXE(icon=...)` embeds an icon into the exe's own PE resources (which is what Windows shows for the taskbar entry), but Qt does not automatically reuse that for the WINDOW's own title-bar icon - nothing in this codebase had ever called `setWindowIcon()`. **Fixed**: added `get_bundled_asset_path()` in `binsifter/core/config.py` - separate from `get_binsifter_root()` (which correctly continues to mean "where persistent, writable data belongs" and must stay stable, per its own docstring's onefile/`_MEIPASS` caveat) - that checks `sys._MEIPASS` first (PyInstaller's own documented, version-independent pointer to wherever `datas` actually landed, regardless of onedir layout or onefile vs. onedir) and falls back to checking next to the exe directly, so it works under the current PyInstaller layout AND wouldn't break again if some future PyInstaller version changes the default a third time. `main_window.py`'s sidebar logo and `about.py`'s logo now both go through this instead of `get_binsifter_root()` directly. Also added a real `self.setWindowIcon(...)` call in `MainWindow.__init__` using the PNG window-icon asset (not the `.ico` `winnow.spec` embeds - PNG decoding is built directly into Qt on every platform, unlike `.ico`, which needs a separate `imageformats` plugin that may or may not be bundled), and added `BinSifter-WindowIcon.png` to `own_datas` so it's actually available at runtime. Also floor-pinned PyInstaller (`pyinstaller>=6.0`) in both `build_winnow.ps1` and the release workflow - the fix above is layout-agnostic either way, but there's no reason to keep inviting the same class of silent, version-triggered behavior change for some other future PyInstaller release too, matching every other real dependency in `pyproject.toml` (all pinned with at least a floor). Covered by 4 new tests in `tests/test_config.py` (prefers `_MEIPASS` when the asset lives there, falls back to the exe's own directory when it doesn't, falls back cleanly when not frozen at all, never raises when the asset is missing everywhere). **Could not be verified against a real Qt render** - this dev sandbox's PySide6 install can't even import `QtGui` at all (missing system `libEGL.so.1`, no root access to install it), so `main_window.py`/`about.py` were checked via direct reading plus `ast.parse()` syntax validation only, not exercised end-to-end. Needs a real Windows re-test to confirm the logos and title-bar icon actually render now.
- [x] **REAL BUG FOUND AND FIXED: a real scan stalled at 651/652 files and produced zero further log output for over 3 hours before being force-closed (Winnow_scanLogs_08142026.txt).** Diffed every `Scanning:` path in the log against every `Finished:` path (652 vs. 651, set difference, not a guess) and found exactly one file that was dispatched and never came back: `WindowsXP-KB936929-SP3-x86-RUS.exe`, a large XP-era self-extracting hotfix installer. Root cause: capa is the ONLY per-file pipeline stage with its own hang-safety net (`PersistentCapaWorker`'s 90s timeout, `capa_scan.py`) - hashing, Authenticode, imphash, ssdeep, YARA, and FLOSS all run directly in the pool worker process with nothing bounding them at all. `scan_directory()`'s result-draining loop (`engine.py`) used a plain, un-timed-out `result_queue.get()` - once a pool worker got stuck inside ANY of those untimed stages for that one file, its callback never fired, so the loop's `for _ in range(submitted): result_queue.get()` blocked on that exact slot forever, which blocked the `with _NoDaemonPool(...) as pool:` block from ever exiting, which blocked the whole scan indefinitely - every other file had already finished, so there was nothing left to make progress on, just an unbounded wait for a worker that was never coming back. **Fixed** by replacing the blocking `result_queue.get()` with a polling loop that tracks how long it's been since the LAST result of any kind arrived (`RESULT_STALL_TIMEOUT_SECONDS = 1200`, 20 minutes - more than double the worst normal per-file time actually observed in that same log, 530.0s for `AcroRd32.dll`, so this shouldn't trip on a genuinely slow-but-progressing file). If nothing comes back for the full ceiling, every still-outstanding file (identified by its placeholder record's `Status` still reading `"Scanning"` - nothing else needed, since a real result always overwrites that) gets marked `Status="Error"` with a clear message, and the loop breaks - the `with _NoDaemonPool(...) as pool:` block's own unconditional `pool.terminate()` on exit (already how a normal scan completion tears the pool down) is what actually kills the permanently-stuck worker. Covered by a new test in `tests/test_engine.py` that replaces `_NoDaemonPool` with a fake, synchronous, in-process stand-in (a real 20-minute wait obviously isn't practical in a test) whose callback is deliberately never invoked for one designated path, confirming that file ends up `Status="Error"` while the other files in the same batch still complete normally. Full suite re-run: 216 passed, same 3 pre-existing unrelated failures. **Not yet re-tested against a real hang** - this fixes the class of bug (any untimed stage hanging on any file), not confirmed against the exact same file, since reproducing whatever specifically made `WindowsXP-KB936929-SP3-x86-RUS.exe` hang (hashing, Authenticode, ssdeep, or YARA - all four are equally unbounded) isn't possible from this sandbox without the actual file or a real Windows/YARA-rules environment.
- [ ] **Still open, not chased this pass: WHICH stage actually hung on that file.** The stall-timeout fix above prevents the class of bug (any single file blocking the whole batch forever) regardless of which stage is at fault, but doesn't identify or fix whatever's actually slow/hanging in hashing/Authenticode/ssdeep/YARA for a file like this - only capa has a real per-stage timeout today. Worth adding `stage_seconds` (already tracked per file, see `_WorkerFileResult`) to whatever record now gets the stall-timeout error, so a future occurrence at least names which stage was in progress when the clock ran out, instead of just "some stage, don't know which."

## Winnow Stop button not actually stopping a running scan - 2026-08-15

Reported directly after the stall-recovery fix above, from the same FLARE VM: clicking Stop mid-scan showed "Stopping..." in the status bar and then just went right back to "Scanning...", repeatedly - had to force-close the app to actually end the scan.

- [x] **REAL BUG FOUND AND FIXED: `should_stop()` was only ever polled in the SUBMISSION loop, which finishes before a human can click anything.** `scan_directory()`'s submission loop checks `should_stop()` once per file, right before dispatching it via `pool.apply_async()` - but `apply_async()` doesn't block on worker availability, so for a normal-sized batch every file gets queued to the pool within milliseconds of the scan starting. Once submission finished, `should_stop()` was never checked again anywhere in the function - the Stop button's flag was real, the GUI's "Stopping..." status was real, but nothing was ever polling it again for the rest of the scan, so it silently reverted back to "Scanning..." exactly as reported. **Fixed** by adding a `should_stop()` check inside the result-draining loop (the same loop the 2026-08-14 stall-recovery fix above already made poll-based instead of a single blocking `get()`), and reducing that loop's poll interval from 30s to 1s so the check is actually reachable at human timescales. When it fires, every still-outstanding file (same `Status == "Scanning"` placeholder-detection the stall path already uses) gets marked `Status = "Cancelled"` (not `"Error"` - this is a deliberate user action) and the pool's own unconditional `pool.terminate()` on `with`-block exit kills the in-flight workers, same mechanism the stall path already relies on.
- [x] **Second real bug found while writing the regression test for the fix above: an early version of the fix could discard already-completed results it just hadn't drained yet.** The first draft checked `should_stop()` unconditionally at the top of every loop iteration, before attempting to read `result_queue`. A new test simulating a worker that finishes synchronously during submission (a realistic case - fast files can genuinely finish before the draining loop even starts) caught this immediately: the already-available result sat unread in the queue while `should_stop()` fired first and cancelled the file anyway, marking a file that had actually SUCCEEDED as `"Cancelled"`. **Fixed** by moving the `should_stop()` check into the `queue.Empty` branch only - a real, already-arrived result is now always drained and applied first; `should_stop()` only ever affects further WAITING for results that haven't come back yet, never a result that's already sitting there.
- [x] **Third real bug found running the regression test against a REAL worker pool (not the fake one): a single, fresh file could still be wrongly cancelled because process spawn-up itself takes longer than one poll interval.** Timed it directly in this dev sandbox: a real `multiprocessing` worker scanning one trivial file took ~2.2 seconds end to end (process spawn + module import overhead alone, before any actual scanning), well over the 1s poll interval the fix above introduced. A `should_stop()` check that fires the instant a single poll comes back empty was cancelling files that were never stuck - just not back yet. **Fixed** by splitting the timing threshold in two: the existing `RESULT_STALL_TIMEOUT_SECONDS` (1200s) still governs genuine hangs, but a new, much shorter `STOP_GRACE_SECONDS` (5s) now separately governs how long the draining loop waits with zero progress before it actually honors a Stop click - comfortable margin over the ~2.2s measured spawn overhead, while still enormously more responsive than the "never" this whole fix exists to replace.
- Covered by a new test in `tests/test_engine.py` (`test_stop_clicked_mid_scan_actually_stops_instead_of_reverting`) using the same fake-synchronous-pool approach as the stall-recovery test, with `RESULT_STALL_TIMEOUT_SECONDS`/`STOP_GRACE_SECONDS` both monkeypatched down so the test stays fast; confirms a file that already completed stays `"Completed"` while files still genuinely in flight when Stop is honored end up `"Cancelled"`. Full suite re-run: 217 passed, same 3 pre-existing unrelated failures as every prior run this session. **Not yet re-tested against a real installer build** - needs a fresh Winnow installer and a real scan, stopped mid-run, to confirm the Stop button now actually ends the scan promptly instead of reverting.

## Winnow footer status bar showing "not installed" for YARA/Capa/SSDEEP - 2026-08-15

Same real-machine report as the Stop button above: the footer at the bottom of Winnow read "YARA: not installed", "Capa: not installed", "SSDEEP: not installed" despite the same scan producing real YARA hits and completing normally - the tools were plainly working, just not reporting a version.

- [x] **REAL BUG FOUND AND FIXED: `importlib.metadata.version()` can't find a package's version metadata inside a PyInstaller-frozen build unless that package's `.dist-info` folder is explicitly bundled - it isn't, by default, even when the package's actual code is bundled and working fine.** `core/tool_metadata.py` reports each tool's version via `importlib.metadata.version(dist_name)` for `yara-python`/`flare-capa`/`ppdeep` - a correct, working approach under a normal `pip install`, where every installed package's `.dist-info` metadata directory sits right next to its code. PyInstaller's static analysis, though, only follows and bundles actual *importable code* by default; unless a hook explicitly asks for it (via `copy_metadata()`), the `.dist-info` folder itself never makes it into the frozen build. Without that folder present, `importlib.metadata.version()` genuinely can't find an entry for the package and raises `PackageNotFoundError` - which `tool_metadata.py`'s own `_package_version()` already catches and reports as `"not installed"`, a reasonable fallback for an *actually*-missing package, just not what was really happening: the code was there and working the whole time, only the metadata lookup was failing. **Fixed** in `installer/winnow.spec`: added `copy_metadata("yara-python")`, `copy_metadata("flare-capa")`, and `copy_metadata("ppdeep")` (PyInstaller's own documented mechanism for exactly this gap), wired into `Analysis()`'s `datas` alongside the existing `mscerts`/`speakeasy`/`unicorn` collections. Confirmed in this dev sandbox (installed `pyinstaller` directly to check, since it isn't normally present here) that all three package names resolve to real `.dist-info` folders via `copy_metadata()` - not just a plausible-sounding fix, verified it actually finds something to bundle. Full suite re-run: 217 passed, same 3 pre-existing unrelated failures. **Not yet re-tested against a real installer build** - needs a fresh Windows build to confirm the footer now shows real version numbers instead of "not installed".

## NSRL cache appearing to rebuild from scratch every run - 2026-08-15

Both FLARE VM scan logs this week showed a ~36-37 minute NSRL cache build, rather than the fast cache-hit path reusing a prior run's cache - user flagged this as a likely contributor to how long scans are taking.

- [x] **Read through nsrl.py's freshness check end to end - the mechanism itself is sound, not obviously broken.** `cache_is_fresh()` compares the source NSRL file's CURRENT `st_mtime_ns`/`st_size` against what got recorded in the cache header at build time - if either differs at all, it correctly (and by design) rebuilds, same two-field check the PowerShell version's own cache already uses. `get_cache_path()`/`_cache_path_for()` puts the cache under `<ReportDirectory>/.bsifter-nsrl-cache/`, and `ReportDirectory` defaults to a stable, real path next to the install (`build_default_config()`) that persists across launches via the Settings cache - so under normal use this should reuse a prior cache correctly, and did in this project's own test coverage. **Most likely explanation, not yet confirmed either way**: the NSRL source file's modified-time or size changed between the two FLARE VM sessions - re-copying, re-downloading, or re-extracting the NSRL text file (each 08-14 test round involved reinstalling Winnow "this morning," which plausibly meant re-staging the desktop files too) would give it a new `mtime`, and the freshness check would correctly (not incorrectly) decide to rebuild - this would be expected behavior, not a bug. **Worth confirming directly**: did the NSRL text file on the FLARE VM's desktop get touched/re-copied/re-extracted between those two scan sessions? If it's the literal same file, untouched, and the cache still rebuilt, that would point to something else and is worth another look.
- [x] **REAL (narrower) BUG FOUND AND FIXED along the way: a blank `ReportDirectory` made the NSRL cache path resolve relative to the process's current working directory instead of a stable location.** `get_cache_path()` never guarded against `ReportDirectory` being blank the way several other `ReportDirectory`-dependent features in `engine.py` already do (archive extraction, CSV report writing) - `ReportDirectory` is a legitimate, supported "off" state. A blank value made `Path("") / ".bsifter-nsrl-cache"` resolve to a plain relative path, landing wherever the OS happened to set the process's CWD at launch for a Start-Menu-shortcut-launched GUI app - not guaranteed consistent between separate launches, the same class of "silently lands somewhere unexpected" bug already found once for Rowan's `$BinSifterRoot` resolution landing on System32. **Not confirmed as what actually happened on the FLARE VM** (`ReportDirectory` has a real, non-blank default and nothing in this report suggests it was cleared), but a real latent gap regardless, fixed defensively: `_cache_path_for()` now falls back to `get_binsifter_data_root() / "Reports"` (the same stable, real default `build_default_config()` already uses) whenever `report_directory` comes in blank. Covered by a new test (`test_blank_report_directory_falls_back_to_stable_location_not_cwd`). Full suite re-run: 218 passed, same 3 pre-existing unrelated failures.

## Rowan silently failing to start a scan on FLARE VM - 2026-08-17

Log showed the scan dying instantly with `Unable to find type [BinSifter.NsrlLoader]` - a type Rowan itself defines via `Add-Type`, with zero elapsed time between "Scan starting..." and the error, and the Settings page having worked completely normally beforehand.

- [x] **REAL BUG FOUND AND FIXED: a genuine Add-Type compile failure would have been completely silent - the app would look and behave normally right up until the first line of code that actually needed the type that failed to compile.** Traced precisely: `Show-MainWindow` builds the ENTIRE application - including the giant `Add-Type -Language CSharp -TypeDefinition` block that defines `NsrlLoader`/`ImphashClusterer`/every other native-hot-path type - inside a dedicated STA runspace created via `New-STARunspace()` (`[RunspaceFactory]::CreateRunspace([InitialSessionState]::CreateDefault())`). That fresh session state does NOT inherit `$ErrorActionPreference = 'Stop'` from the outer script's own top-level scope (only variables explicitly passed via `SessionStateProxy.SetVariable()` cross that boundary, and this isn't one of them) - inside this runspace, `$ErrorActionPreference` silently defaults back to PowerShell's own `'Continue'`. That means if `Add-Type` ever hit a genuine C# compile error here, on any machine, for any reason, it was ALWAYS just a non-terminating warning: execution kept running straight through to the main window and Settings page (both fully functional, matching exactly what was reported), and the real compiler diagnostic only ever reached `$ps.Streams.Error` - which only gets read out at all (via a bare `Write-Warning`, not even a dialog) AFTER `[Application]::Run($form)` returns, i.e. after the user closes the whole app. The only way this could ever have surfaced before now was hours later, as a completely unrelated-looking "type not found" error the first time anything actually tried to USE one of the types that silently failed to compile - exactly the report this fixes, regardless of what the underlying compile problem on that specific FLARE VM turns out to be (the C# source itself was read start to end and shows no obviously version-incompatible syntax - no `switch` expressions, `??=`, `using var`, target-typed `new()`, or `record` types, none of which PowerShell 7's bundled compiler reliably supports - so the actual trigger is still unconfirmed and may be environmental, e.g. Defender or a temp-directory permission issue interfering with the C# compiler's own output). **Fixed**: added `-ErrorAction Stop` to the `Add-Type` call plus a `try`/`catch` around it that shows a real `MessageBox` immediately, with the actual compiler diagnostic text attached, and returns cleanly instead of continuing into a half-broken app. This doesn't identify the exact original compile problem (still unknown - this dev sandbox has no `pwsh`/Roslyn to compile-test the ~1200-line C# block directly, and no network access to install one), but it turns an untraceable, hours-later, wrong-looking symptom into an immediate, actionable error naming the real problem - the next real test run will surface the actual diagnostic needed to close this out for good. Bracket/brace/paren balance re-verified via a custom PowerShell-aware scanner (here-strings/quoted strings/comments treated as opaque, matching the project's established diff-verification practice) plus direct visual confirmation of both edit sites (the `if`/`try`/`catch` nesting is small and fully readable by inspection). **Not yet re-tested against a real scan** - needs a fresh installer build and another FLARE VM run; whatever the dialog shows this time is the actual next step.

## Winnow authenticode stage averaging 61.8 SECONDS per file - real root cause found, 2026-08-17

The FLARE VM's first full 652-file scan ran over 4 hours. This is the first scan that ever completed cleanly enough (thanks to the 2026-08-14 stall-timeout fix) to produce the per-stage timing summary `engine.py` has logged since that same date - and that summary immediately named the problem:

```
authenticode    40218.6s total  ( 92.5%)  over   651 file(s)  -  avg 61779.7 ms/file
ssdeep           2263.5s total  (  5.2%)  over   548 file(s)  -  avg  4130.5 ms/file
imphash           799.7s total  (  1.8%)  over   548 file(s)  -  avg  1459.3 ms/file
hash               96.8s total  (  0.2%)  over   651 file(s)  -  avg   148.7 ms/file
yara               80.7s total  (  0.2%)  over   548 file(s)  -  avg   147.2 ms/file
capa                8.2s total  (  0.0%)  over     1 file(s)  -  avg  8203.8 ms/file
```

- [x] **REAL BUG FOUND AND FIXED (in a third-party dependency, not BinSifter's own scan logic): authenticode was 92.5% of all worker CPU time - roughly 400x slower per file than hashing or YARA on the exact same files.** This scan had `CatalogDirectory` pointed at a real Windows CatRoot folder with 5730 real `.cat` files (the catalog-verification feature added 2026-08-08, whose own TODO note in `authenticode.py` already flagged "only tested against synthetic data, not a genuine catalog end-to-end" - this was that end-to-end test, and it found a real problem). `check_signature()` calls `signed_file.add_catalog(catalog, check=True)` once per configured catalog, per file - up to 5730 times per file here. Read directly in signify 0.9.2's own installed source (`signify/authenticode/signed_file/base.py`): `add_catalog(..., check=True)` calls `catalog.find_subject(self)`, which looks up the file's hash in `CertificateTrustList._subjects` / `._subjects_by_indirect_data_hash` (`signify/authenticode/trust_list.py`) - both plain `@property`, with **zero caching**: `return {subj.identifier_str: subj for subj in self.subjects}`, where `.subjects` itself re-decodes every ASN.1 `trusted_subjects` entry in that catalog from scratch, on literally every single call. Confirmed this is the real, exact call path (not just plausible) by reading `add_catalog()`'s own source line: `if not check or catalog_.find_subject(self):`. Reproduced the bug mechanically before fixing it: a fake `CertificateTrustList` subclass with a call-counting `.subjects` stub showed 5 accesses to `._subjects` triggering 5 full recomputes with no caching, confirming the mechanism precisely. **Fixed** in `binsifter/core/authenticode.py`: patches `CertificateTrustList._subjects` and `._subjects_by_indirect_data_hash` to `functools.cached_property` at import time (once per worker process, before any file is scanned) - confirmed `CertificateTrustList` has no `__slots__`, so this works with no other changes. This is exactly correct, not just an expedient shortcut: every real `CertificateTrustList` instance here is already long-lived by BinSifter's own design (`parse_catalogs()` parses each `.cat` file's raw bytes exactly once per worker, then hands the same object to every `check_signature()` call that worker makes for the rest of the scan - this bug was a SECOND, hidden re-parse happening one level deeper than that existing reuse ever prevented). Re-ran the same call-counting reproduction after the patch: 5 accesses now trigger exactly 1 recompute. Covered by a new test, `test_certificate_trust_list_subjects_are_cached_not_reparsed_every_call`, in `tests/test_authenticode.py`. Full suite re-run: 219 passed, same 3 pre-existing unrelated failures. **Not yet re-tested against a real scan with a real catalog corpus** - needs a fresh installer build and another FLARE VM run against the same real CatRoot folder to confirm authenticode's per-file cost drops back down near hashing/YARA's own (sub-second) range.
- [x] **Secondary, smaller, expected (not a bug) contributor identified in the same log: the NSRL source file itself indexed 432,866,777 hashes - about 6x larger than the ~72 million assumed in nsrl.py's own original design notes.** `NSRL cache built: 432866777 hash(es) indexed (2117.2s)` - 35.3 minutes, a real cost, but a ONE-TIME cost per source file (the whole point of the 2026-08-04 mmap-cache rewrite): every scan after the first against this exact file reuses the cache instead of re-parsing. Not changed this pass - noted here so the size of this NSRL file is on record for future performance comparisons, since it's meaningfully outside the range every earlier design/benchmark note in this project assumed.

## Dashboard/nav-button text skewed and not fitting at higher display scaling - 2026-08-17

Reported from the FRED workstation test - a scaled display made dashboard tile and button text "skewed" and not fit properly, in both variants. Both variants use the exact same underlying design pattern (fixed pixel Location/Size for every label, copied 1:1 from the original design - Winnow's `widgets.py` says so directly in its own header comment), but the two GUI frameworks needed genuinely different fixes, not the same one twice.

- [x] **REAL BUG FOUND AND FIXED (Rowan/WinForms): no DPI-awareness declared anywhere at all.** Confirmed via a direct search for `DpiMode`/`HighDpi`/`AutoScale`/`dpi` across the whole file - zero matches, before this fix. Without it, Windows falls back to bitmap-stretching the whole UI on a scaled display rather than letting WinForms rescale its own controls properly - exactly the "skewed, doesn't fit" symptom reported. **Fixed**: added `[System.Windows.Forms.Application]::SetHighDpiMode([System.Windows.Forms.HighDpiMode]::PerMonitorV2)` right after the `System.Windows.Forms`/`System.Drawing` assemblies load and before `EnableVisualStyles()` (Microsoft's own documented required ordering), plus `AutoScaleMode = [System.Windows.Forms.AutoScaleMode]::Dpi` on all three `Form` instances in the file (the main window, the Sigcheck/Speakeasy report viewer, and the archive-password dialog). This is WinForms' own built-in, complete fix for this exact scenario: once a form knows it's per-monitor-DPI-aware, `AutoScaleMode.Dpi` automatically rescales every already-positioned child control's Location/Size by the real runtime-vs-design DPI ratio - no changes needed to any of the individual hardcoded tile/button pixel values themselves.
- [x] **REAL BUG FOUND AND FIXED (Winnow/PySide6): a real, different bug - Qt doesn't auto-rescale a widget's fixed geometry the way WinForms does, so this needed a different fix, not a Qt equivalent of SetHighDpiMode.** Qt6 already enables High-DPI scaling by default (unlike Rowan, nothing was missing there), and it does scale rendered fonts and physical pixels together - but a `QLabel`'s `setGeometry(x, y, w, h)` box stays fixed in logical pixels regardless of what its font ends up needing at the system's actual scale factor. At clean integer scale factors (100%/200%) this mostly goes unnoticed; at the FRACTIONAL scale factors Windows recommends by default on most real scaled displays (125%/150%/175%), font-metric rounding and logical-pixel rounding for a hand-picked constant like `160x18` can diverge just enough to clip or crowd text that fit fine at 100% - a real, if narrower, class of bug than Rowan's, needing correction inside `binsifter/gui/widgets.py` itself rather than one global app-level flag. **Fixed**: added `_ensure_label_fits()` in `widgets.py` - after a label's real font and text are both set, it grows (never shrinks below the original hand-tuned design size, so a 100%-scale render looks unchanged) the label's box to whatever `QFontMetrics` says the CURRENT font+text actually needs, with a small margin. Wired into every text label in `StatTile` (caption/value/subtitle, both compact and regular layouts) and `NavButton`'s sidebar label - the two components the report named directly (dashboard tiles, nav "buttons"). `StatTile.set_value()` now also re-fits on every update, not just once at construction - fixes a second, DPI-independent latent bug the same mechanism: the value label was originally sized to fit a couple of digits, so a large real scan count (e.g. "12,483") could already have clipped at 100% scaling too, this just happened to surface as the same symptom. **Could not be verified against a real Qt render** - this dev sandbox's PySide6 install still can't import `QtGui` at all (missing system `libEGL.so.1`, no root access, same limitation noted for every prior Winnow UI fix this project) - checked via `ast.parse()` syntax validation and direct code reading only. Rowan's edits verified via the same quote/comment-aware bracket/brace/paren balance scanner used for every other Rowan-only change (no new imbalance introduced). Full Python test suite re-run: 219 passed, same 3 pre-existing unrelated failures (no Qt-render-dependent tests exist for `widgets.py` in this sandbox, same as every prior UI change here). **Neither side has been re-tested against a real scaled display** - needs a real Windows re-test at the same scaling percentage (125%/150%) that surfaced the original report.

## Fourth real Winnow/Rowan test round - 2026-08-18

Fresh scans on host PC and FLARE VM for both variants. Rowan completed cleanly on both machines. Winnow's FLARE VM scan is still ~4 hours; two logs were provided specifically to compare a first run (cold NSRL cache) against an immediate follow-up (warm cache), plus a Winnow host PC log for a three-way comparison. The user's own hypothesis was that this looked like a FLARE-VM-specific issue rather than a Winnow bug.

- [x] **Deep-dive confirms the user's instinct was half right, in an important and useful way: it IS environment-specific, but not because FLARE VM is inherently slower - it's the exact same catalog re-parse bug identified and fixed yesterday, and it only manifests on a machine where CatalogDirectory actually resolves to real catalogs.** Host PC's log: `Loaded 0 catalog file(s) from C:\Windows\System32\CatRoot` - authenticode averaged a normal 45.7ms/file, 391s (6.5 min) total scan. Both FLARE VM logs: `Loaded 5119 catalog file(s) from C:\Windows\System32\CatRoot\{F750E6C3-...}` - authenticode still ~85% of all CPU time, ~40-43 SECONDS/file, 155-200 minutes total. These two FLARE VM runs predate the user rebuilding/reinstalling with yesterday's `functools.cached_property` fix (the ~40s/file numbers are the same order of magnitude as yesterday's pre-fix 61.8s/file, not anywhere near what a working fix should produce) - so this round doesn't yet confirm or refute that fix, it just reconfirms the same root cause is still active because the installed build hasn't changed yet.
- [x] **REAL BUG FOUND AND FIXED along the way: the host PC's 0-catalogs result isn't "correctly has nothing to check" - CatalogDirectory was pointed one level too high to find anything, a real, silent correctness gap, not just an incidental performance difference.** Windows' actual catalog store is `C:\Windows\System32\CatRoot\{GUID}\*.cat` - every real `.cat` file lives one level BELOW the well-known, obviously-named `CatRoot` folder, inside a much less discoverable GUID-named subfolder. Pointing `CatalogDirectory` at the bare `CatRoot` path (the far more natural first guess for anyone setting this up, and exactly what the host PC install did) makes `parse_catalogs()`'s non-recursive `directory.glob("*.cat")` correctly, but silently, find nothing - no error or warning ever reached the GUI, only a `Loaded 0 catalog file(s)` log line nobody was watching for. That's not merely "catalog checking happens to be off" - every catalog-signed system binary on the host PC that should have resolved to `Valid` landed in some other status instead, unnoticed. **Fixed** in `binsifter/core/authenticode.py`'s `parse_catalogs()`: if the configured directory has no `*.cat` files directly inside it, it now also checks exactly one level of subdirectories (`*/*.cat`) - matching CatRoot's real on-disk layout exactly, not an unbounded recursive walk (deliberately bounded to avoid turning a misconfiguration into a slow full-tree scan if CatalogDirectory ever pointed somewhere much larger). Both the bare `CatRoot` folder and its GUID subfolder now work identically, with zero configuration change needed on any machine already set up either way. Covered by 2 new tests in `tests/test_authenticode.py` (finds catalogs one level down when the top level has none; still prefers top-level catalogs when both exist, so the fallback never triggers on an already-correct setup). Full suite re-run: 221 passed, same 3 pre-existing unrelated failures. **Not yet re-tested against a real host PC scan** - needs a fresh installer build to confirm the host PC now actually checks its own real catalogs (and, combined with yesterday's caching fix, does so quickly).

## Rowan DPI fix from 2026-08-17 didn't actually work - real cause found, 2026-08-18

Real screenshots at 150%/175% display scaling on the host PC showed the exact same truncated text yesterday's `SetHighDpiMode`/`AutoScaleMode.Dpi` fix was supposed to prevent - the top-bar buttons literally read "Settin", "Hel", "Ab" (their original 100%-scale pixel widths, unchanged), and Settings-page body text was cut off mid-sentence. The FLARE VM, running at 100% scale, showed no issue - confirming this really is scale-triggered, not something else, and that yesterday's fix had no real effect.

- [x] **REAL BUG FOUND AND FIXED: `AutoScaleMode.Dpi` alone is a no-op without also setting `Form.AutoScaleDimensions` - and this whole UI, built entirely by hand with no WinForms Designer, never had that baseline set anywhere.** `AutoScaleMode.Dpi` rescales every child control by comparing the CURRENT runtime DPI against `AutoScaleDimensions`, a baseline dimension the form is expected to record as "the DPI/font size this form's pixel values were actually designed at." A Designer-generated form gets this stamped automatically inside `InitializeComponent()` (`this.AutoScaleDimensions = new SizeF(96F, 96F);`, set before any child controls are added) - a hand-built form, like this entire file, has no such statement anywhere, and WinForms doesn't infer one on its own. With no baseline recorded, `PerformAutoScale()` has nothing meaningful to compare the runtime DPI against, so it computes an effective scale ratio of 1.0 and does nothing - every control's hardcoded Location/Size stayed exactly as authored, regardless of `AutoScaleMode` being set correctly. This explains precisely why yesterday's fix looked complete (both required WinForms APIs were used, in the documented order) but had zero visible effect on a real scaled display. **Fixed**: added `AutoScaleDimensions = New-Object System.Drawing.SizeF(96, 96)` immediately after `AutoScaleMode = Dpi` on all three `Form` instances (the main window, the Sigcheck/Speakeasy report viewer, the archive-password dialog) - 96x96 being the real, standard baseline every hardcoded pixel value in this file was actually chosen against (100% scale = 96 DPI), matching what a Designer would have stamped automatically. Balance re-verified via the same PowerShell-aware bracket/brace/paren scanner used for every other Rowan-only change (no new imbalance). **Not yet re-tested against a real scaled display** - needs a fresh installer build and another host-PC test at 150%/175% to confirm the top-bar buttons and Settings-page text now actually fit instead of clipping to their original 100%-scale widths.

## Rowan DPI fix from 2026-08-18 ALSO didn't work - real cause found via a live diagnostic script, 2026-08-19

A fresh installer with the 2026-08-18 `AutoScaleDimensions` fix, installed and tested on the same 175%-scaled host PC, produced the exact same truncated text ("Settin", "Hel", "Ab") as both prior attempts - confirmed by another real screenshot. Two consecutive fixes reasoned entirely from Microsoft's own documented WinForms behavior had now both failed on real hardware, so rather than guess a third time, a standalone diagnostic script (`diagnose_dpi.ps1`, run directly via `pwsh -File diagnose_dpi.ps1` - no build or install needed) was written to get real runtime evidence first. This is now the established approach for any future GUI-behavior fix in this project: cheap diagnostic script and real console output before a code change, not documentation-only reasoning.

- [x] **REAL ROOT CAUSE FOUND: `Form.AutoScaleDimensions` was already silently reading the CURRENT runtime DPI (168x168 on this 175%-scale machine) by the time any of this project's code or even a manual `PerformAutoScale()` call ran - not the 96x96 baseline the 2026-08-18 fix stamped.** Three rounds of diagnostic output, all captured directly from the real host PC: round 1 confirmed `SetHighDpiMode(PerMonitorV2)` returned `True`, `GetProcessDpiAwareness` independently confirmed `PER_MONITOR_DPI_AWARE`, and `Form.DeviceDpi` correctly read 168 - every documented prerequisite was genuinely satisfied, yet a test button's `Size` was byte-for-byte identical (126x44) before and after `Show()`. Round 2 added a manual `$form.PerformAutoScale()` call as a direct fix candidate - ruling out an automatic-trigger-timing explanation (e.g. `WM_DPICHANGED` never firing on a single-monitor machine), since calling the exact same underlying method by hand also produced no resize, and printed `AutoScaleDimensions` immediately before that call: already `168 x 168`, not the `96 x 96` explicitly stamped in code. That's the real explanation for both failed fixes - something in this PowerShell 7 / .NET WinForms combination re-stamps `AutoScaleDimensions` to match live DPI before this project's code (or even a manual call) ever gets to use the 96x96 value it set, so `PerformAutoScale()` computes ratio = 168/168 = 1.0 and correctly decides there's nothing to do. Round 3 tested the actual fix candidate: a fresh, never-touched test button, scaled directly via `Control.Scale(SizeF)` - the lower-level, unconditional method `PerformAutoScale()` is documented to call internally - with a ratio computed straight from `DeviceDpi/96.0` (bypassing `PerformAutoScale()`'s gating entirely). Result: `126 x 44` -> `220 x 77`, matching the expected 1.75x ratio exactly. **Fixed** in `BinSifter-Rowan_v1.3.0-beta.1.ps1`: added a manual `$form.Scale((New-Object System.Drawing.SizeF($dpiRatio, $dpiRatio)))` call (ratio from `$form.DeviceDpi / 96.0`) inside the main form's `Add_Shown` handler, guarded by a one-shot flag so a second `Shown` firing (e.g. after minimize/restore on some Windows builds) can't double-scale everything - placed before `Move-TopBarControls`, since that function positions the top-bar buttons from their own (now correctly scaled) `.Width`. The same fix was applied to the other two hand-built forms in the file (the Sigcheck/Speakeasy report viewer, the archive-password dialog - the latter has genuine `Location`-based rows and was exactly the kind of layout this bug hits hardest), each via their own `Add_Shown` handler right before their `ShowDialog()` call. The `AutoScaleMode`/`AutoScaleDimensions` lines from the two prior attempts were left in place (harmless, still provide DPI-aware font metrics) but their comments were corrected to stop claiming they're the fix and point to this section instead. Balance re-verified via the same PowerShell-aware bracket/brace/paren scanner used for every other Rowan-only change this dev sandbox can't run `pwsh` to check directly (0 unmatched braces/parens across the whole file after the edit - the scanner itself needed a bug fix mid-session too, to correctly skip backtick-escaped `` `" `` sequences inside double-quoted strings, which it hadn't handled before and was producing false-positive imbalance reports on completely unrelated, unchanged code as a result). Full Python test suite re-run: 221 passed, same 3 pre-existing unrelated failures (Rowan has no automated test suite of its own - this dev sandbox has no `pwsh` to run one). **Not yet re-tested against a real scaled display** - needs a fresh installer build and another host-PC test at 150%/175% to give the final, empirical confirmation that the top-bar buttons and Settings-page text now actually fit.

## Done

- [x] **Fix engine.py's missing pipeline gating (2026-08-05, validated 2026-08-06).** `engine.py` was running imphash/ssdeep/YARA on every file regardless of NSRL match, and capa on every format-eligible file regardless of YARA hit count - on the 652-file/1-YARA-hit scan, capa should have run on at most 1 file and ran on 549. Fixed to match `BinSifter-Rowan_v1.3.0-beta.1.ps1:2208-2459`: imphash/ssdeep/YARA now wrapped in `if not record.NsrlMatch:`; `file_type_mod.classify()`/`CapaEligible`/the capa call now nested inside `if record.YaraHitCount > 0:`. FLOSS's own gate (`file_type.py:79`) already correctly checked `yara_hit_count > 0`, untouched. Covered by two new tests in `test_engine.py`. **Confirmed against a real scan** (`Loom_scanLogs_08052026-1221.txt`): capa ran on exactly 1 file (matching YARA Hits: 1, down from 549), YARA/ssdeep/imphash all ran on 549 files (652 minus the 103 NSRL matches, down from 652), hash/authenticode stayed at 652 (correct - those are unconditional by design). Pool wall-clock dropped from 2378.8s to 359.5s.
- [x] **ssdeep per-file cost investigated (2026-08-06).** Root cause confirmed via direct source read + benchmark, not a BinSifter bug: `ppdeep`'s `_spamsum()` core is a pure-Python per-byte loop, no C acceleration, measured at ~850-890ms/MB (~1.16 MB/s) directly. That's inherent to the library, not fixable without swapping it out. Applied the one safe, no-tradeoff win: `compute_ssdeep_hash()` now uses `ppdeep.hash_from_file(path)` instead of `open()+read()+ppdeep.hash(data)`, avoiding one full redundant in-memory buffer copy (`ppdeep.hash()` re-wraps bytes in a `BytesIO` and re-reads it in 64KB chunks regardless of how it got the data). Real savings on large files, not a rounding error, but doesn't change the fundamental ~1.16 MB/s ceiling - swapping `ppdeep` for a C-accelerated ssdeep binding would be the only way to meaningfully move this number, and isn't attempted here.
- [x] **Authenticode trust store corrected (2026-08-05/06).** The "BinSifter doesn't populate a real root CA trust store, expect NotTrusted not Valid" claim baked into `authenticode.py`'s docstring/TODO was wrong - never actually verified, just assumed. Re-checked by reading signify 0.9.2's source directly: `AuthenticodeSignature.verify()` defaults `trusted_certificate_store` to `TRUSTED_CERTIFICATE_STORE`, a real Microsoft root bundle shipped via the `mscerts` package (backed by Microsoft's actual `authroot.stl`). `check_signature()`'s zero-argument `explain_verify()` call never overrides that default, so it was already getting real trust-chain validation with no code change needed. **Confirmed end-to-end against a real scan** (`Loom_scanLogs_08052026-1336.txt` / `BinSifter_Triage_2026-08-05_133031.csv`): 94/652 files came back `Valid` with real signer names (Lenovo, Adobe, Honeywell, etc.), 0 came back `NotTrusted` - the fix works in production, not just in theory.
- [x] **Authenticode "UnknownError" misclassification found and fixed (2026-08-06).** Same real scan's status breakdown: `NotSigned`=371, `UnknownError`=187, `Valid`=94. That 187 is not "the tool is broken" - broke it down by extension and found two distinct, fixable causes: (1) **75 `.msi` files** - signify's MSI parser (`SignedMsiFile`) needs the optional `olefile` dependency, which BinSifter never declared, so `AuthenticodeFile.from_stream()` couldn't even attempt MSI parsing and raised `ParseError` before ever reaching a real verdict. Fixed: `pyproject.toml` now depends on `signify[msi]` instead of bare `signify`; confirmed in-sandbox that installing `olefile` makes `SignedMsiFile` register as a real `AuthenticodeFile` subclass. (2) **61 scripts (`.cmd`/`.bat`/`.ps1`) + 15 `.docm`** - none of these are formats signify can parse at all (no PE/MSI/flat-signature-file container), so the same `ParseError` fires - but that was being caught by `check_signature()`'s broad catch-all and reported as `UnknownError` instead of the already-existing, more accurate `NotSupportedFileFormat` status. Fixed: `authenticode.py` now catches `signify.exceptions.ParseError` specifically and maps it to `NotSupportedFileFormat`. Covered by a new test (`test_unparseable_format_is_not_supported_not_unknown_error`). **Still open: 36 `.dll`/`.exe` files** (genuinely PE-parseable formats) were also `UnknownError` - `check_signature()` only logs these at `logger.debug` and doesn't persist the reason anywhere, so root cause is unknown without re-running with debug logging enabled. Not chased further this pass.

## Performance tuning

- [x] ~~capa timeout tuning~~ - moot now that capa runs on at most a handful of YARA-hit files per scan (1 file on the last real run). Historical data (120s/90s/60s comparisons) kept in git history if ever needed again at a different YARA-hit volume.
- [x] ~~ssdeep hot path~~ - see Done section above; `hash_from_file()` applied, remaining cost is inherent to pure-Python `ppdeep`.

## Correctness / trust

- [x] **Exception-classification fix confirmed against a real scan (2026-08-06, `BinSifter_Triage_2026-08-05_145310.csv`).** `UnknownError` is now 0 across all 652 files, down from 187 - every file that used to land there now correctly shows `NotSupportedFileFormat` instead, with `Valid`=94 and `NotSigned`=371 unchanged from before. Confirms the `signify.exceptions.ParseError` catch is working exactly as intended in production, not just in the unit test.
- [x] **`olefile` / `signify[msi]` fix confirmed after reinstall (2026-08-06, `BinSifter_Triage_2026-08-05_151536.csv`).** Reinstalled (`pip install -e .`) and re-ran - 73 of the 75 `.msi` files now resolve to real verdicts: 4 `Valid` (real signers - LLC 1C-Soft, Adobe Systems) and 69 `NotSigned`. Dashboard's "Signed" tile moved 94 -> 98 (matches the 4 newly-Valid MSIs exactly). Overall `NotSupportedFileFormat` dropped from 187 to 114. **2 `.msi` files are still unparseable** - folded into the item below rather than tracked separately, since it's the same class of problem (signify's parser rejecting a file it should probably handle).
- [x] **RESOLVED 2026-08-08 - not a bug. All 38 files confirmed to genuinely not be valid PE/MSI files; `NotSupportedFileFormat` is the correct classification.** `diagnose_authenticode.py` run against the real 576-file `F:\Binaries2Scan` set confirms the "not really MZ-prefixed" hypothesis exactly, with no exceptions: **36 of the 38 are literal 0-byte files** (`ACT20.EXE`, `SCANSTATE.EXE`/`LOADSTATE.EXE`, `SUPTOOLS.MSI`, `WBEMODBC.MSI`, `XPSPREQS.DLL`, etc. - all recognizable legitimate Windows/third-party utility names, strongly suggesting a copy/collection failure during evidence gathering rather than anything wrong with the files as they exist on the source system) - `header = f.read(16)` returns nothing, `starts_with_mz`/`starts_with_ole` are both `False`, and signify correctly can't identify a file with zero content as any known type. **The remaining 2 are non-empty but genuinely not PE/MSI content at all**: `Instruktsiya_pejo_4007.exe`'s first bytes decode to `<br />\n<b>Warnin` - literally the start of an HTML/PHP warning page, meaning whatever downloaded this "exe" actually received an error page instead of a real payload (a common failure mode when scraping/downloading from a malware distribution site that's down, rate-limited, or serving a CAPTCHA) - worth flagging as an artifact in its own right, not just a parse failure. `SanTint.Systems.Conn.dll`'s first bytes (`2f 30 32 7a 39 6d 4d 55 50 48 45 3d...`) look like base64 or other encoded text, not any recognizable binary header - possibly a corrupted/incomplete download or an encoded dropper stage that never got decoded to disk. **No code change needed** - `check_signature()`'s existing `ParseError` -> `NotSupportedFileFormat` mapping is working exactly as designed; these files really are unsupported formats, not a signify parser limitation. **Worth considering as a small follow-up, not done here without confirming first:** BinSifter currently can't distinguish "genuinely 0-byte file" or "extension doesn't match real content" from the general `NotSupportedFileFormat` bucket, but both are meaningful, distinct signals for a triage analyst (a 0-byte file usually means "check your evidence collection," a mismatched-content file can itself be an IOC) - could be worth a dedicated status value if this comes up again on a future case, but not built speculatively. Confirmed via two consecutive real scans that this is a stable, reproducible set, not scan-to-scan noise. Original working theory (last pass): "legacy NT4/2000/XP-era PE binaries with header conventions that predate something signify's modern parser assumes." Traced through signify 0.9.2's actual source this pass (`signify/authenticode/signed_file/{base,pe,msi}.py`) rather than re-guessing, and that theory doesn't hold up: `AuthenticodeFile.from_stream()` only raises `ParseError` when **every** registered subclass's `_try_open()` returns `None` - and `SignedPEFile._try_open()` does nothing more than check that the file's first 2 bytes equal `"MZ"`; it doesn't parse any PE structure at all at that stage, so a genuinely old/unusual-but-real PE header can't fail here no matter how non-standard its `SizeOfOptionalHeader`/section characteristics are - `explain_verify()` (called later) already catches exactly that kind of downstream parse failure internally and resolves it to `NotSupportedFileFormat` without ever raising back up to `check_signature()` (confirmed by `AuthenticodeVerificationResult.call()` in `verification_result.py` catching `ParseError`, which `SignedPEParseError` subclasses). **Reproduced this empirically, not just from source**, with a synthetic file whose PE structure is completely broken past the "MZ" prefix (no valid section table, garbage optional header) - it resolves cleanly to `NOT_SIGNED`/similar, never raises. **New, better-supported hypothesis: the 36 files most likely don't actually start with the literal bytes `4D 5A` at all** - something else is going on (truncation, corruption, a reparse point/symlink resolving oddly, or genuinely not a PE despite the `.exe`/`.dll` extension) - `_try_open()` doesn't look far enough into the file for "old but valid PE" to be a plausible explanation anymore. For the 2 `.msi` files, a parallel but distinct mechanism: `SignedMsiFile._try_open()` checks the full 8-byte OLE compound-file magic, and if THAT matches but `olefile.OleFileIO()` still can't open it as a real OLE container (deeper validation than the magic bytes alone), `_try_open()` raises internally and `from_stream()` moves on - so these 2 could be OLE-magic-prefixed but otherwise malformed/truncated MSI files specifically, not "signify doesn't understand an old MSI variant." New `diagnose_authenticode.py` (repo root) turns this into a 10-second, no-guessing check against the REAL files: prints each file's first 16 bytes in hex plus the exact `check_signature()` outcome (including the full underlying exception, not just the `logger.debug`-level line the real scan pipeline logs) - run it against a directory containing the actual 38 files (or the whole SrcDir) and the hex dump alone will immediately confirm or refute the "not really MZ-prefixed" theory. **2026-08-08: script re-verified functional, hypothesis further corroborated with two more synthetic cases (no real progress possible beyond this without the actual files, which still aren't reachable from this sandbox - checked scan_heuristics/, Reports/, smoketest/, and uploads, none contain the actual 38 files or a matching triage CSV).** Ran `diagnose_authenticode.py` against 3 fresh synthetic files: a file starting with `MZ` followed by garbage (as before, resolves cleanly to `NOT_SIGNED`, never raises `ParseError` - confirms the mechanism again), a file NOT starting with `MZ` at all (raises `ParseError: Unable to determine file type with available parsers` exactly as the new hypothesis predicts), and a genuinely empty (0-byte) file (also raises the same `ParseError`, a case not tried before - a 0-byte file is itself a plausible real-world explanation for some of the 38, e.g. a failed/interrupted copy during evidence collection). All three outcomes match the hypothesis exactly, so the tool is confirmed working correctly and ready to run. **Still needs to be run against the real files** - this dev sandbox has no access to them, so the theory is well-supported by source + synthetic reproductions but not yet confirmed against the actual `SCANSTATE.EXE`/`LOADSTATE.EXE`/`SUPTOOLS.MSI`/etc. Not fixed this pass either way (there's nothing to fix yet without knowing which of the above it actually is) - `diagnose_authenticode.py`'s output is what determines whether this needs a real code fix or turns out to be something entirely outside BinSifter's control (corrupted source files, bad copies, etc.).
- [x] **Dashboard "Unsigned" tile reworked, final form (2026-08-06) - now in both Rowan and Winnow.** Went through two iterations before landing: (1) originally fired on `SignatureStatus != "Valid"`, lumping genuinely-unsigned files together with unparseable-format files and real check failures into one misleading number; (2) briefly split into "Not Signed" + "Not Verifiable" tiles, which didn't hold up visually (6 tiles broke column alignment with the 5-tile row below it); (3) **final: reverted to a single tile, but reporting the positive count instead** - renamed to **"Signed"**, counting `SignatureStatus == "Valid"`. Simpler to read, keeps the 5-tile row intact, and every non-Valid status is implicitly "not signed or not verified" without needing its own tile or explanation. Winnow: `dashboard_stats.py`'s field is now `signed` (was briefly `not_signed`/`not_verifiable`, originally `unsigned`); `dashboard.py`'s Enrichment Summary top row is back to 5 tiles (Imphash Clusters, Signed, Known-Bad, Files With IOCs, Escalated). Covered by `test_signed_only_counts_valid_status` in `test_dashboard_stats.py`. Rowan: `$tileUnsigned`/`$UiTotals.Unsigned` renamed to `$tileSigned`/`$UiTotals.Signed` throughout (tile definition, click predicate, dirty-queue diff snapshot, refresh-timer text assignment), predicate inverted to `-eq 'Valid'`, replicated by hand to keep both variants' dashboards consistent (Rowan has no automated test suite to catch a mismatch, so this needs a visual check next time Rowan runs).
- [x] **Dashboard color pass (2026-08-06) - both variants, visually confirmed.** "Signed" and "NSRL Matches" recolored to `theme.Success` (green, `RGB(83,201,91)` dark / `RGB(30,160,90)` light - the same green already used elsewhere for "Ready"/"Completed" status, not a new color) since both are "trustworthy/accounted for" signals rather than neutral Accent-blue info. "Files With IOCs" recolored to `theme.Warning` (orange) to match "YARA Hits" - both are "found something worth a look" signals of the same weight. Applied identically to Winnow (`dashboard.py`) and Rowan (`BinSifter-Rowan_v1.3.0-beta.1.ps1`). **Confirmed via screenshot from a real Winnow scan** (2026-08-06): 5-tile top row restored, Signed (94, green) and NSRL Matches (103, green) both render correctly, Files With IOCs shows orange matching YARA Hits - approved.
- [x] **Catalog-based (.cat) signature verification - implemented 2026-08-08 (Winnow only - see the dedicated section below for why Rowan needed zero changes).**

## Quick-launch context menu (Results grid right-click)

Three issues found doing a real right-click pass over the menu, 2026-08-06.

- [x] **Speakeasy/Sigcheck quick-launch could hang the whole app - real thread-safety bug, fixed (2 rounds).** `results.py`'s `_start_captured_tool()` connected `worker.finished`/`worker.failed` to plain lambdas with no explicit connection type. PySide6 only auto-detects a cross-thread connection by inspecting the *receiver*'s QObject thread affinity through the bound method a signal is connected to - a lambda isn't a QObject-bound slot, so that detection can't see through it, and PySide6 silently falls back to a DirectConnection: everything the lambda calls (`_teardown_tool_thread()`'s `thread.wait()`, and `_show_tool_report()`'s `QDialog`/`QMessageBox` construction) ran on the background worker thread instead of the main thread. That's a thread calling `.wait()` on itself (self-deadlock) plus cross-thread widget parenting - exactly matches what was hit: `QThread::wait: Thread tried to wait on itself` and `QObject::setParent: Cannot set parent, new parent is in a different thread`, app then unresponsive short of Task Manager. **Round 1 fix** (explicit `type=Qt.ConnectionType.QueuedConnection` on the same lambda `.connect()` calls) did not hold - the identical crash hit again running Sigcheck (same shared method), plus new `QBasicTimer::stop` spam. A lambda still isn't a QObject Qt can resolve a receiver-thread from, connection-type override or not. **Round 2 fix**: stopped connecting to lambdas at all - `worker.finished`/`worker.failed` now connect directly to genuine bound methods of `self` (`_on_captured_tool_finished`/`_on_captured_tool_failed`), the one connection shape PySide6's auto-detection is actually documented to handle correctly. `self.sender()` identifies which worker fired (one shared slot now serves every concurrent run), and a plain `worker.title` attribute (set before `moveToThread`/`thread.start()`, so no cross-thread write) replaces what the lambda closure used to carry. This affected **both** Sigcheck and Speakeasy (same shared code path). Winnow-only - Rowan's WinForms events don't have this footgun. No automated test (GUI/Qt code isn't unit-tested in this project - no display in the dev sandbox). **Confirmed working end-to-end (2026-08-06)** - a real test pass ran Sigcheck (clean report), Ghidra headless (legible confirmation), and Speakeasy (legible warning dialog, emulation completed, results reported) all in one pass with no hangs, no crashes, no terminal spam. Round 2 held.
- [x] **Ghidra headless gave zero feedback on success - fixed in both variants.** Confirmed via Rowan's source that this wasn't a Winnow-only regression - Rowan's `Start-Process` is equally silent on success, only showing a MessageBox on the error paths (missing Ghidra config, missing Report Directory, launch exception). The complaint ("no other windows open, no messages... appears nothing happens") is valid either way. Added a one-time, non-blocking confirmation dialog after a successful launch in both `results.py`'s `_launch_ghidra()` and the PS1's `$ghidraItem.Add_Click` handler, telling the user analysis started and where results will land (`<ReportDirectory>\ghidra_projects\<project_name>`). Headless analysis still runs unattended after that with no progress indicator by design (unchanged) - this is just a "yes, it started" acknowledgment.
- [x] **Sigcheck grayed out - investigated, not a bug.** Checked the Tools folder before guessing at a fix (the obvious hypothesis - Sysinternals ships Sigcheck as two separate files, `sigcheck.exe`/`sigcheck64.exe`, and BinSifter's `TOOL_FILE_NAMES` only searches for the literal 32-bit name - would have been a real gap). Turned out neither file was present at all; "not configured" was the correct, honest state. Both have since been copied into the Tools folder - should resolve on the next scan/Settings re-save with no code change needed. Worth revisiting the bitness-fallback idea (search for `sigcheck64.exe` too, preferring it) as a small defensive improvement if this ever comes up again for real, but not implemented since there's no confirmed case that needs it yet.
- [x] **`QMessageBox` text illegible (near-black on near-black) - separate bug from the Speakeasy thread issue, fixed.** Screenshots of the Ghidra success dialog (the new one just added above) and the Speakeasy confirmation dialog both showed body text almost unreadable against the background. Confirmed this is unrelated to the threading fix above: the Ghidra dialog is a plain synchronous `QMessageBox.information()` call with zero threading involved, yet shows the identical symptom. Root cause: `binsifter/gui/__main__.py` never applies any app-wide `QApplication.setStyleSheet()`/`setPalette()` (confirmed via grep - genuinely nothing exists) - every other widget in the app gets its colors from its own explicit per-widget stylesheet, but `QMessageBox.information/warning/critical()` calls (used everywhere - `main_window.py`, `results.py`, `settings.py`) get zero BinSifter theming and fall through entirely to Qt's own rendering. On a Windows 11 machine with OS-level dark mode on, Qt6 auto-detects that and applies a dark palette to native dialogs, but the auto-derivation has a well-known gap where the background darkens correctly while text-color roles don't lighten to match - not a BinSifter bug in the usual sense, but a real, user-visible defect either way. Fixed with one app-wide stylesheet in `__main__.py` (`_apply_message_box_stylesheet()`) targeting `QMessageBox`/`QMessageBox QLabel` with the same theme colors used everywhere else - covers every current and future `QMessageBox` call site in one place, immune to the OS auto-detection quirk. Winnow-only; Rowan's WinForms `MessageBox.Show()` uses a completely different native rendering path with no equivalent bug reported on Rowan. **Confirmed fixed (2026-08-06)** - screenshots of the Ghidra and Speakeasy dialogs both show fully legible text now.

## GUI theming

- [x] **Winnow now auto-detects OS dark/light mode, matching Rowan - previously always looked dark regardless (2026-08-06).** Directly asked whether Winnow adapts to the OS theme "like in Rowan" - checked, and it didn't: `main_window.py` hardcoded `self.theme = DARK` unconditionally, and both `main_window.py`'s sidebar logo and `about.py`'s About page logo hardcoded the dark-mode logo file (`BinSifter-Logo-Horizontal-Dark.png`) - `theme.py`'s `LIGHT` palette and `get_theme_palette()` dispatcher existed but were never actually called anywhere. Rowan, by contrast, genuinely detects the OS setting via `Test-SystemDarkMode()` (reads `HKCU\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize\AppsUseLightTheme` once at startup, falls back to light on any read failure) and picks both its palette and logo file from that. Ported the same detection to Winnow: `theme.py`'s new `detect_os_dark_mode()` reads the identical registry value/key so both variants agree on what "the OS theme" means (not some different Python-native mechanism that could disagree with Rowan on an edge case), falling back to light if `winreg` isn't importable (non-Windows) or the read fails for any reason - same graceful-degrade behavior as Rowan's `catch { return $false }`. New `logo_horizontal_filename(theme)` picks between the two logo assets that already existed in the repo (`BinSifter-Logo-Horizontal-Dark.png`/`BinSifter-Logo-Horizontal.png`) the same way Rowan's bootstrap does. Wired through: `__main__.py` detects once and shares the result with both `MainWindow(theme)` and the QMessageBox stylesheet fix from earlier (previously hardcoded to `DARK`, so message boxes would have mismatched the rest of the app in light mode) - detecting in two separate places risked them disagreeing. Like Rowan, this is a one-time check at launch, not a live toggle - changing the OS theme while either variant is running needs a relaunch to pick up, matching Rowan exactly rather than adding new behavior Rowan doesn't have. **Confirmed via screenshots with the OS in light mode (2026-08-06)** - Dashboard and About page both render the light palette correctly, and the light-mode logo (`BinSifter-Logo-Horizontal.png`, not the `-Dark` variant) is showing in both the sidebar and About page. Dark mode was already confirmed in every earlier screenshot this session (the OS default). Both paths now verified.

## Archive/compressed-file support

- [x] **Archive/compressed-file support (zip/tar/gzip/7z) - requested 2026-08-06, implemented 2026-08-07 in both Winnow and Rowan.** New `core/archive.py` module + wiring into `engine.py`'s `scan_directory()` (Winnow); a direct port of the same design into the PS1's `Start-ScanEngine` dispatcher (Rowan, see below for the one real implementation difference). Three open design questions from the original ask were put directly via AskUserQuestion rather than guessed at; all three are now built exactly as confirmed, in both variants:
  1. **Format scope: zip, tar (+ compressed-tar variants), standalone gzip, and 7z.** RAR was explicitly NOT included - the confirmed choice was the zip/tar/gzip/7z option, not the "also add RAR" one; RAR would need an external `unrar`/`unar` binary the same way Sigcheck/Ghidra need a configured `.exe`, deferred until there's a real need. Winnow: Python stdlib `zipfile`/`tarfile`/`gzip` for the first 3 (no new dependency), `py7zr` (new dependency) for 7z. Rowan: all 4 formats go through one tool, 7-Zip's own CLI (`7z.exe`, new `SevenZipExe` entry in `$ToolFileNames` - discovered under ToolsDir the same as every other tool), rather than a separate library per format - matches Rowan's existing "shell out to a real tool" convention (yara64.exe/capa.exe/ssdeep.exe/etc.) instead of translating Winnow's specific implementation choice.
  2. **Password-protected archives: a pre-scan pass finds every locked archive under SrcDir before the real per-file scan starts, prompts for all of them at once in a single batch dialog, then extracts with whatever passwords were supplied** - not a per-file mid-scan interruption. Archives nobody has a working password for get copied (never moved - the analyst's original evidence under SrcDir is never touched) into `<ReportDirectory>/password_protected/` for external cracking tools (John/hashcat/etc.). Winnow: `gui/archive_password_dialog.py`'s `ArchivePasswordDialog`. Rowan: a WinForms port of the same dialog, `Show-ArchivePasswordDialog`, next to `Show-ToolReportWindow`.
  3. **Extracted files show up in Results as their own ordinary rows, not grouped/nested under the archive's row** - `FileRecord.SourceArchive` (new field in both variants' `FileRecord`, also a new CSV column and Results grid column, "Source Archive") is blank for a directly-scanned file and holds the immediate containing archive's path for an extracted one (the immediate parent, not necessarily the top-level archive, if archives are nested a few levels deep).

  **The architecture question flagged as the hardest part turned out to have a much simpler answer than it looked, in both variants.** The original concern was "how does a background worker pop a dialog on the GUI thread and block for a password" - genuinely looked unsolved when this was scoped. Working through it: archive expansion doesn't need to happen inside the per-file worker pool at all, in either variant. It runs as a **serial pre-scan step**, in the exact same place NSRL/blocklist/YARA/capa already get loaded once, before the pool/runspace-pool is created. **Winnow**: that pre-scan step already runs on a background **QThread** (`main_window.py`'s `_ScanWorker`), in the *same process* as the GUI, not a separate `multiprocessing.Pool` process - a QThread can signal the GUI thread and block on a `threading.Event` for an answer using ordinary, safe Qt cross-thread coordination. **Rowan**: the dispatcher runs in its own PowerShell *runspace* (a thread, not a process, sharing the same in-process memory as the GUI runspace) - rather than a direct cross-runspace `Control.Invoke()` (untested and genuinely risky to get right blind, with no Windows/WinForms environment available to verify it in), this reuses the SAME polled-shared-state pattern the dispatcher already relies on for pause/stop/progress: `$ScanControl` (a `[hashtable]::Synchronized(...)`) grows 3 new fields (`PendingPasswordRequest`/`PasswordMap`/`PasswordMapReady`), the dispatcher sets the request and polls-and-blocks on `PasswordMapReady`, and `$refreshTimer.Add_Tick` (the same 750ms UI-thread timer that already drains logs/dirty-record queues) notices the pending request, shows `Show-ArchivePasswordDialog`, and writes the answer back. Same "block a background worker, let the UI thread answer" shape as Winnow, built out of each variant's own already-proven cross-thread/cross-runspace primitives rather than either forcing one variant's mechanism onto the other or inventing a third, unverified one. By the time either variant's worker pool actually starts, every extracted file is just an ordinary file on disk with a real path - the pool/runspace-pool workers themselves needed **zero changes** in either variant.

  Two sub-questions that weren't put directly to a decision (format scope / password-prompt architecture / Results display were judged the three load-bearing ones) got documented defaults instead of blocking on an answer, same in both variants:
  - **Nested archives** (a zip inside a zip) are expanded recursively, bounded by a max nesting depth of 3 (`core/archive.py`'s `MAX_NESTED_DEPTH` / the PS1's `$MaxNestedArchiveDepth`) - a zip inside a zip inside a zip inside a zip is left as a plain extracted file past that point, not further opened. Easy to change later, not worth blocking on.
  - **Extracted files live under `<ReportDirectory>/extracted_archives/<archive-stem>_<short-hash>/`**, kept after the scan (not a temp dir cleaned up afterward) - same "persisted next to Reports" convention `ghidra_projects`/`generated_rules` already use, and needed anyway so Ghidra/Sigcheck/Speakeasy quick-launch (which need a real, still-existing file path) can be pointed at anything extracted from an archive.

  **Winnow**: covered by `tests/test_archive.py` (31 tests: format classification, password detection for zip/7z, extraction, wrong-password handling, nested recursion including the depth-cap boundary, name-collision handling in the unresolved-archives directory) plus a new end-to-end regression test in `tests/test_engine.py` (`test_archive_contents_scanned_through_real_worker_pool_with_source_archive_set`) that runs a real archive through the real `multiprocessing.Pool` rather than just `archive.py` in isolation - this caught a real bug during manual verification (see below) that an `archive.py`-only unit test never could have. Full test suite: 161 passed, same 3 pre-existing/environment-specific failures as before this work (confirmed unrelated by re-running them against the unmodified `engine.py` from HEAD - identical failures either way): a capa-rules-corpus gap in the dev sandbox, a `multiprocessing`-spawn/`monkeypatch` interaction that predates this change, and a missing sandbox fixture file.

  **Real bug caught and fixed during Winnow's end-to-end verification (2026-08-07, before this ever shipped):** `SourceArchive` was originally set on the placeholder `FileRecord` dict built *before* the pool ran, then silently clobbered the instant each file's real result came back from a worker (`records[result.record.Path] = result.record` replaces the whole object, since each pool worker builds its own fresh `FileRecord` with no knowledge of `source_archive_by_path`) - a real scan against a zip showed every extracted file's `SourceArchive` coming back blank despite the assignment code existing elsewhere in the function. Fixed by applying `source_archive_by_path` *after* the pool's completion-draining loop instead of before it starts; confirmed via a manual end-to-end run and the new regression test above, both passing. **This bug could NOT have happened in Rowan** - PowerShell runspaces share one process's memory, so every worker mutates the SAME shared `FileRecord` object in place (`$FileRecords[$FilePath].SomeField = ...`) rather than a spawned Python worker PROCESS building and returning a separate copy that has to be merged back in; Rowan sets `.SourceArchive` once, at record-creation time, with no equivalent clobbering risk to guard against.

  **Rowan verification caveat, stated plainly:** this dev sandbox is Linux-only with no interactive Windows/WinForms environment, so - unlike Winnow, which was exercised with real end-to-end scans - the Rowan port could only be verified by careful manual read-through plus mechanical checks (brace/paren/bracket balance before and after each edit, confirming every new function is defined exactly once, cross-checking `Invoke-ExternalTool`/`$ScanControl`/`$refreshTimer` call signatures by hand against their real definitions elsewhere in the file). `7z.exe`'s exact CLI behavior (the `-slt`/`Encrypted = +` password-detection heuristic in particular) was corroborated against 7-Zip's own documented behavior, not tested against a real `7z.exe` binary. **A real scan against a small test folder with a plain zip and a password-protected one should be run before trusting this in a real case** - flagging this explicitly rather than presenting it with the same confidence as the Winnow half, which WAS run for real.
- [x] **Rowan's archive feature confirmed working end-to-end against real, live-malware-family zips (2026-08-08).** Tested with 7 password-protected zips downloaded from Malware Bazaar (a real-world, adversarial-content test, not a synthetic sample). Two real, separate findings from that run:
  1. **`7z.exe` wasn't found under the configured ToolsDir at first** - Rowan correctly detected this and logged the graceful-skip message ("SevenZipExe (7z.exe) is not configured under ToolsDir - archive expansion is being skipped this run"), exactly as designed (same convention as every other missing-tool case, e.g. the earlier Sigcheck investigation) - not a bug, just genuinely not configured yet. Resolved by copying `7z.exe` into ToolsDir.
  2. **With `7z.exe` in place, the password prompt fired correctly and archive expansion ran** - the first real confirmation of the whole batch-password-prompt/cross-runspace-polling design (`$ScanControl.PendingPasswordRequest`/`PasswordMap`/`PasswordMapReady`, `Show-ArchivePasswordDialog`) actually working on real Windows/WinForms, not just surviving manual read-through. The scan didn't fully complete because **Windows Defender quarantined the extracted files immediately after extraction** - expected, correct AV behavior for real malware samples extracted to disk, not a BinSifter bug ("an oversight on my behalf" per the report). A FlareVM re-confirmation (Defender disabled, built for this exact purpose) is planned when available - noted as still-pending, not blocking, since the parts of the pipeline that DID run (archive detection, password prompt, extraction) already worked.
- [x] **Shared/batch password field added to the password-prompt dialog, both variants (2026-08-08).** Added right after confirming the batch-prompt flow works: when several archives share one password (the Malware Bazaar case here - all 7 zips used the same password), typing it once beats filling in the same value per-row. Added a "Shared password (optional)" field above the per-archive list in both `binsifter/gui/archive_password_dialog.py` (Winnow, `ArchivePasswordDialog`) and the PS1's `Show-ArchivePasswordDialog` (Rowan). Semantics kept deliberately simple and identical in both: an archive's own per-row field wins if filled in (so one oddball archive with a different password can still be entered individually without clearing the shared field first); otherwise the shared field's value is used for that archive. An archive with neither filled in still falls through to `password_protected/` for external cracking, unchanged. Winnow: `password_map()` updated, no dedicated test (GUI/Qt code isn't unit-tested in this project, same as the rest of this dialog). Rowan: `$sharedPanel`/`$sharedField` added via the same Location-based layout pattern already used for the per-archive rows (not `Dock`, since `TextBox` doesn't respect `Padding` under Dock the way `Label` does), dialog height bumped 480->540 to fit; `$passwordMap` build loop now falls back to `$sharedField.Text`. Brace/bracket/paren balance re-verified after edit (1187/1187 braces, 1048/1048 brackets, parens at the same pre-existing -1 baseline). Not yet run for real on Rowan - worth confirming alongside the FlareVM re-test above.
- [x] **Winnow's zip extraction couldn't decrypt AES-encrypted zips - real bug, found and fixed 2026-08-08.** The next real test (Malware Bazaar zips again, this time getting past the `7z.exe`/Defender issues from the prior two rounds) hit a new one: shared password entered correctly, but every archive logged "Could not unlock... with the supplied password: That compression method is not supported," and none of the 7 zips' contents got extracted or scanned - only the 7 zip containers themselves showed up in results.
  - **Root cause, confirmed by reading stdlib `zipfile`'s own source AND reproducing the exact error against a synthetic fixture before touching any code:** Malware Bazaar's zips use WinZip AE-x (AES-256) encryption, not legacy ZipCrypto. Python's stdlib `zipfile` module can open/list an AES-encrypted zip fine (reading the central directory doesn't need the decryption key - confirmed `needs_password()` correctly flagged all 7 as locked and the password prompt fired correctly), but `_check_compression()` doesn't recognize AE-x's compress_type marker (99) at all and raises `NotImplementedError("That compression method is not supported")` the instant actual extraction is attempted - regardless of whether the supplied password is correct. `core/archive.py`'s `_extract_zip()`/`resolve_locked_archives()` treat any exception during extraction as "wrong password" (by design, since they're not meant to distinguish further - see that function's docstring), so this surfaced as every archive looking like a bad password even though the password was right.
  - **Fix:** swapped `_extract_zip()` from stdlib `zipfile.ZipFile` to `pyzipper.AESZipFile` (new dependency, `pyproject.toml`) - a drop-in superset with the same `extract()`/`infolist()`/`setpassword()` API that adds real AE-1/AE-2 AES decryption, while still handling legacy-ZipCrypto and unencrypted zips exactly the same way stdlib zipfile did (no behavior change for anything that already worked - confirmed via the existing `_make_password_zip`/ZipCrypto tests still passing unchanged). `needs_password()` deliberately left on stdlib `zipfile` - it only reads `flag_bits`, never needs to decrypt, so there was nothing to fix there.
  - **Rowan needed no equivalent change** - confirmed by the prior test above (see the archive end-to-end entry): Rowan shells out to `7z.exe` for every archive format, and 7-Zip has native AES-zip support built in, which is exactly why that earlier Rowan run extracted successfully (up to the unrelated Defender-quarantine issue) while this same class of file failed in Winnow.
  - **Tests:** `tests/test_archive.py` gained a `_make_aes_zip()` fixture builder (via `pyzipper` directly, since the `zip` CLI used for the existing ZipCrypto fixture can't write AES entries either) plus two new tests - `needs_password()` still correctly detects an AES zip as locked, and `resolve_locked_archives()` now actually extracts one given the right password (this second test is the real regression guard: it reproduces the exact bug and would fail against the old stdlib-zipfile code). Full suite: 172 passed (up from 170), same 3 pre-existing/unrelated failures as every prior run this cycle.
- [ ] **Ingot (Rust variant).** Revisit whenever - no active work planned.

## Release readiness (2026-08-08)

- [x] **Winnow promoted to Beta - `2.0.0a1` -> `2.0.0b1`.** README's Variants table/Status section rewritten: Winnow was still described as "GUI in progress" / "placeholder shell," both stale - the real GUI (Dashboard, Results, Scan Queue, Settings, Logs, YARA/capa rule management, Help, About) has existed for a while and has now been run end-to-end against real malware. README also gained: a "Getting started"/"Requirements" split covering both variants (previously Rowan-only), and a link to NIST's [RDSv3 to RDSv2 text files conversion guide](https://s3.amazonaws.com/rds.nsrl.nist.gov/RDS/RDSv3_Docs/RDSv3_to_RDSv2_text_files.pdf) next to the NSRL requirement (BinSifter's NSRL loader expects RDSv2 text, NSRL itself now ships RDSv3 - this link was specifically requested, since it's not an obvious thing to go find on your own).
- [x] **Windows Defender exclusion feature - both variants.** Added directly after the real Malware Bazaar scan where Defender's real-time protection raced BinSifter's own worker pool for extracted archive contents (see the archive-support section above). New Settings-page button ("Add extraction folder to Defender exclusions...") in both variants, gated behind an explicit confirmation dialog spelling out the tradeoff (Defender won't auto-flag anything in that folder) before ever firing a UAC prompt - deliberately opt-in, never something a scan does on its own.
  - **Winnow:** new `core/defender.py` (`add_exclusion_path()`) - spawns a SEPARATE elevated `powershell.exe` via `Start-Process -Verb RunAs` (Windows' own UAC prompt is what elevates, BinSifter's own process never gains admin rights) running `Add-MpPreference -ExclusionPath` via `-EncodedCommand` (avoids 3-level quoting hell). Distinguishes "UAC declined" (Win32 `ERROR_CANCELLED`/1223) from "Add-MpPreference itself failed" (wrong AV product active, Tamper Protection, etc.) with different messages. `tests/test_defender.py` covers the non-Windows guard and the subprocess-plumbing/exit-code-interpretation logic via monkeypatched `subprocess.run` - the real Windows elevation flow itself couldn't be exercised from this Linux sandbox, stated explicitly in both the module docstring and here.
  - **Rowan:** new `Add-DefenderExclusionPath` function, same design ported to native PowerShell (no need to shell out to get a PowerShell interpreter - Rowan already runs in one), using `[System.Diagnostics.Process]::Start()` with `Verb='runas'` instead of `Start-Process` directly so the UAC-declined case (`Win32Exception` with `NativeErrorCode -eq 1223`) can be caught precisely. Brace/bracket/paren balance re-verified after both edits (1202/1202 braces, 1065/1065 brackets, parens at the same pre-existing -1 baseline).
  - **Not yet run for real on either variant** - same caveat as everything else touching UAC/elevation this project has hit before: no Windows environment in this dev sandbox to click through an actual UAC prompt. Worth a real test before relying on it.
- [x] **Frozen-exe path resolution fixed ahead of the installer work.** Found while writing `winnow.spec`: `main_window.py`/`about.py` resolved the sidebar/About-page logo PNGs via a hardcoded `Path(__file__).resolve().parent.parent[.parent]` chain, and `config.py`'s `get_binsifter_root()` (which anchors Reports/Attack/Blocklist/the settings cache) did the same - both assume `binsifter/` sits on disk as loose `.py` files, which stops being true once frozen by PyInstaller. Worse than a cosmetic missing-logo bug: under a `--onefile` build specifically, the equivalent `sys._MEIPASS`-based fix would have pointed `get_binsifter_root()` at a FRESH temp-extraction folder on every single launch, silently breaking Settings-cache persistence and forcing an NSRL cache rebuild every run. Fixed by making `get_binsifter_root()` frozen-aware (`sys.executable`'s own directory when `sys.frozen`, stable across launches - not `sys._MEIPASS`) and having `main_window.py`/`about.py` call that single shared function instead of each independently computing their own relative-parent chain (also fixes a latent drift risk - the two files disagreed on parent-chain depth, 3 vs 4, only correct because they started at different nesting depths). This is also *why* `winnow.spec` builds `--onedir`, not `--onefile` - see that spec's comments. Full test suite unaffected (178 passed, same 3 pre-existing failures).
- [x] **Installer scaffolding added for both variants - `installer/` (new folder).** Goal: real, installable Beta releases. Per the confirmed choices (2026-08-08): Winnow bundles everything (PyInstaller `--onedir` freeze, wrapped in Inno Setup); Rowan does NOT bundle PowerShell 7 or 7-Zip, just checks for `pwsh.exe`/`7z.exe` on PATH post-install and links to their official installers if missing (same "check and tell, don't silently fail" pattern already used for every other optional external tool in this codebase). Both installers: Inno Setup wizard, license page (`LICENSE`), installable without admin rights (`PrivilegesRequired=lowest` + the modern per-user/all-users override dialog), Start Menu shortcut, an **unchecked-by-default "Create a desktop icon" checkbox** (per the "create desktop icons, or ask the user if they want one" requirement - the checkbox IS the asking), and deliberately does NOT delete Reports/case data on uninstall.
  - **Files:** `winnow.spec` (PyInstaller), `Winnow.iss` + `Rowan.iss` (Inno Setup), `build_winnow.ps1` + `build_rowan.ps1` (orchestration), `README.md` (prerequisites + the caveat below, stated up front rather than buried).
  - **`winnow.spec` specifically calls out the packages most likely to need real iteration**, not just a generic "PyInstaller might need tweaking" disclaimer: `mscerts` (signify's Microsoft trust-store data - missing this wouldn't crash the build, it would silently make every embedded signature check come back untrusted, exactly the class of bug already found and fixed once this project via the 2026-08-06 authenticode.py correction) and `speakeasy` (its own runtime data files) both get explicit `collect_all()`; `vivisect`/`envi`/`capa` (capa's own well-known-fragile dynamic-import dependencies) get `collect_submodules()`. `upx=False` deliberately - UPX-compressed executables are a known antivirus false-positive trigger, a real concern for a tool that will routinely run on machines with active real-time protection (same theme as the Defender-exclusion feature above).
  - **Explicit, un-hedged caveat (also in `installer/README.md`, not just here): neither installer has actually been compiled/run.** This dev sandbox is Linux-only with no Windows environment to run `ISCC.exe`/`PyInstaller` against at all. The PyInstaller spec reflects real, specific knowledge of this dependency stack's known trouble spots, not guesses - but a stack this size (PySide6 + capa + vivisect + speakeasy/unicorn + signify + numpy) essentially never freezes clean on the first attempt for anyone, sandbox constraints or not. Expect to iterate at least once, and treat this as a solid first draft to build from rather than a finished, verified deliverable - explicitly not presented with the same confidence as work that WAS run for real this session (the archive feature, the AES-zip fix, etc.).
- [x] **GitHub Actions CI added to actually publish these as GitHub Releases - `.github/workflows/release-installers.yml` (new file).** Clarified the goal is real GitHub Releases (the platform feature), with both installers attached as downloadable assets - not just installer scripts sitting locally in the repo. Two hard constraints ruled out doing this from here directly: no Windows environment in this dev sandbox (same limitation as the installer scaffolding above), and separately **no git push or GitHub API credentials at all** (`git push --dry-run` fails "could not read Username for 'https://github.com'"; no `gh` CLI installed). Confirmed both explicitly before proposing the workaround rather than assuming.
  - **Design:** three jobs. `build-winnow` and `build-rowan` run on `windows-latest` GitHub-hosted runners (a real Windows machine, just not this dev sandbox's) and run the exact same steps `build_winnow.ps1`/`build_rowan.ps1` do by hand (pip install, PyInstaller, `choco install innosetup`, ISCC), uploading each installer as a build artifact. `release` runs only when triggered by a pushed `v*` tag (not by `workflow_dispatch`), downloads both artifacts, and runs `gh release create` with `--prerelease`, using the runner's own built-in `GITHUB_TOKEN` - no credentials that need to be managed by hand, and no third-party marketplace action dependency.
  - **Two trigger paths, both documented in `installer/README.md`'s new "Publishing a real GitHub Release" section:** `workflow_dispatch` (Actions tab -> "Run workflow") builds both installers and leaves them as downloadable Actions artifacts without creating a Release - the way to test the build pipeline itself, given neither installer has been compiled even once (see caveat above). Pushing a tag matching `v*` (e.g. `git tag v2.0.0-beta.1 && git push origin v2.0.0-beta.1`) does the same build AND publishes the Release.
  - **Requires action from outside this dev sandbox** - there are no credentials here to push the workflow file, installer files, or a version tag. `git add -A && git commit && git push` this work, then either run the workflow manually first (recommended, to shake out the first-compile issues flagged in the installer caveat) or push a tag directly to cut a real beta release.
  - **Not yet run even once** - same honesty standard as the installer scaffolding itself: this workflow has been written with real knowledge of GitHub Actions/`gh release create`/Windows-runner conventions, but has not been triggered, so treat the first real run as a first attempt, not a verified pipeline.

## AI-assisted triage exploration (local LLM, prototype only)

- [x] **CONCLUDED 2026-08-08 - not viable on real hardware, parked (not deleted).** `prototype_ollama_triage.py` (repo root), deliberately never wired into engine.py/either GUI. Before starting work on Ingot, the idea came up to explore whether a locally-running model (via Ollama) could read BinSifter's already-extracted findings for a suspicious file (YARA hits, capa detections, ssdeep/imphash clustering, Authenticode status, extracted IOCs, ATT&CK mappings) and produce a useful hypothesis about likely intent - fully offline, since case data (live malware samples and their findings) shouldn't leave the analyst's machine.
  - **What it does:** reads an existing `report.py`-produced triage CSV, filters to non-NSRL-matched rows by default (same "don't re-check known-good files" logic `engine.py` already uses to gate YARA/capa/ssdeep behind `not record.NsrlMatch`), builds a compact per-file JSON summary (empty/zero fields dropped, long fields truncated), and sends it to a local Ollama server's `/api/chat` endpoint with a **schema-constrained** `format` parameter (Ollama's structured-outputs feature, backed by XGrammar - the model's response literally cannot fail to validate against the schema, so it always parses cleanly). Writes a new CSV (`<input>_ai_assessment.csv`) with `AI_`-prefixed columns (`AI_LikelyCategory`, `AI_Confidence`, `AI_InsufficientSignal`, `AI_NotableSignals`, `AI_Rationale`) - prefixed specifically so these can never be confused with BinSifter's own deterministic fields if this becomes a real feature later.
  - **Deliberately does NOT** send any raw file bytes (only the same text already shown in the Results grid), call anything but the local Ollama endpoint (fully offline), or treat the model's output as a verdict - the system prompt explicitly instructs it to say `insufficient_signal: true` rather than invent a plausible-sounding story from sparse findings, since this is a hypothesis for further investigation, not a detection.
  - **Only needs the Python standard library** - no `requests`/`ollama` pip package, since it's a plain HTTP POST to `http://localhost:11434`. Has a `--dry-run` flag that builds and prints every payload without calling Ollama at all, useful both for reviewing exactly what would be sent before installing anything, and for how this was verified in this dev sandbox (see below).
  - **Verified in this dev sandbox:** CSV parsing/filtering/compaction logic tested against a synthetic 3-row CSV matching `report.py`'s real column schema (one NSRL-known-good row correctly excluded by default, one YARA/capa/IOC-rich row correctly compacted, one sparse row correctly left mostly empty) via `--dry-run`. The actual Ollama HTTP call path was exercised too (confirmed it fails cleanly with a clear "is Ollama running?" message on connection refused) but **could not be tested against a real running Ollama server or a real model response** - no Ollama installation in this Linux dev sandbox. Needs a real run against a real triage CSV, with Ollama actually installed and a model pulled, before there's any real signal on output quality.
  - **2026-08-08, real machine-specific gotcha found and resolved (not a bug in this script or in Ollama's structured-output feature - confirmed via reading `ollama serve`'s own verbose logs):** on real hardware (GTX 1070 Ti, Pascal/compute 6.1), Ollama's bundled CUDA backend crashed the underlying `llama-server` process with a Windows access violation (`0xc0000005`) on every model tested (`qwen2.5:7b` AND `llama3.2:3b`), reproducible even via plain `ollama run` with no script involved at all - ruled out this script, the JSON schema, and the specific model as causes before looking at the GPU layer. Root cause isolated by comparing `ollama serve`'s verbose startup log across attempts: `OLLAMA_NUM_GPU=0` did NOT prevent GPU offload in this Ollama version (still showed `offloaded 29/29 layers to GPU`); setting `CUDA_VISIBLE_DEVICES=-1` DID work, but not by forcing CPU-only as expected - it hid the CUDA device specifically, and Ollama fell back to its **Vulkan** compute backend on the same physical GPU instead, which loaded and generated successfully (confirmed via full `resolve_fused_ops`/timing output in the log, ~20 tokens/sec, real completion) with flash attention still enabled and all layers still offloaded - so the crash is specific to Ollama's bundled CUDA runtime on this Pascal-generation card, not to GPU acceleration, flash attention, or model choice in general. **Fix: `[Environment]::SetEnvironmentVariable('CUDA_VISIBLE_DEVICES', '-1', 'User')` set as a persistent user env var** (not just in one PowerShell session), so Ollama's normal background/tray app picks it up too, not only a manually-run `ollama serve`. Worth remembering if this project ever runs Ollama on different hardware later - the fix here is GPU/driver-specific, not something to assume applies universally.
  - **Verdict: not the Vulkan-workaround success it looked like.** After switching to the Vulkan backend to work around the CUDA crash above, running the real prototype against a live triage CSV triggered a full GPU/display failure - all monitors went dark, and the machine needed a hard power cycle to recover. That is categorically worse than the earlier access-violation crash: a crashed process is contained and recoverable, a system-wide display lockup requiring a hard reset is not, and repeatedly forcing sustained GPU inference load onto hardware that has already done that once is not a reasonable way to keep testing a prototype - possible causes range from thermal/power-delivery issues to genuine GPU degradation, none of which are worth chasing just to finish a feasibility test.
  - **Conclusion: local-LLM-assisted triage is NOT being pursued as a real BinSifter feature, at least not on this hardware, and not planned further right now.** This is a legitimate, useful outcome of a feasibility test, not a failure of the exploration - the whole point of prototyping before committing was to find out something like this before it became a shipped feature that could do the same thing on an analyst's machine mid-case, which would be a far worse place to discover it. `prototype_ollama_triage.py` is left in the repo (not deleted) as a reference in case this gets revisited on different/newer hardware later, but should NOT be run again on this machine without a specific reason to retest, and definitely not against a large batch of files unattended.

## AI export (Markdown + JSON, both variants) - 2026-08-08

The safer follow-up to the AI-assisted triage exploration above: instead of running any AI *inside* BinSifter (the approach that led to the GPU/display lockup), BinSifter now formats one file's already-extracted findings into a compact Markdown document and a matching JSON object, for the analyst to hand to whatever AI they want - paste the Markdown into a cloud chat interface, or feed the JSON to a script hitting a local model's own API. BinSifter itself never runs or calls out to any AI here, cloud or local - this is pure data formatting, with none of the inference risk, network calls, or new attack surface the prototype above had. Implemented in both variants in the same pass, per the standing convention.

- [x] **Winnow: `binsifter/core/ai_export.py` (new module).** `compact_record()` strips a `FileRecord` down to only the fields worth an AI's attention - empty string/0/`False`/-1 (BinSifter's generic "not computed" sentinels) are dropped, plus one field-specific sentinel (`YaraSeverity`'s `"Unknown"` default, which means the same "nothing to assess" thing `YaraSeverityScore`'s -1 does, just spelled as a word). `Path`/`MD5`/`SHA1` are always kept as the file's identity even if empty; `Disposition` is always kept even at its `"Untriaged"` default, since that's a real triage state rather than a "not computed" placeholder. Long fields (raw capa output especially) truncate at 4000 characters. `build_json()` wraps the compacted fields in a `_meta` block (generation timestamp, a disclaimer that no AI analysis has run on this data - only automated findings) so the JSON is self-explanatory to a script consuming it with no surrounding context. `build_markdown()` renders the same data as a human-readable document with Signature/YARA/capa/ssdeep-imphash/IOC/Other sections, each one only appearing if it actually has something to show. `export_file()` writes both files, named by SHA1 (falling back to the filename stem if a file errored before hashing) - the same convention `_launch_ghidra()` already uses for project folders, so an AI export and a Ghidra project for the same file are easy to cross-reference. Covered by `tests/test_ai_export.py` (10 tests, all passing) - compaction/sentinel-dropping, truncation, JSON structure/round-trip, sparse vs. rich Markdown rendering, and file-naming (SHA1 and stem-fallback).
- [x] **Winnow: wired into Results' right-click context menu.** New "Export for AI analysis (Markdown + JSON)" item in `results.py`'s `_build_context_menu()`, right after Speakeasy, following the same single-selected-row pattern as Ghidra/Sigcheck (`_on_table_context_menu` forces single-row selection, target path read from column 0, record looked up by a linear scan of `self._records`). Unlike Ghidra/Sigcheck/Speakeasy, this has no external tool to configure - the only prerequisite is a Report Directory to write into (same requirement the "Open report folder" toolbar button already has), so the menu item is enabled whenever `self._config.ReportDirectory` is set, with no separate exe-path check. Output goes to `<ReportDirectory>\ai_exports\BinSifter_<SHA1 or filename stem>.md` / `.json`. A `QMessageBox.information()` confirms success with both file paths, matching the Ghidra success dialog's style.
- [x] **Rowan: ported field-for-field into `BinSifter-Rowan_v1.3.0-beta.1.ps1`.** New functions `Test-AiExportSentinelValue`, `Get-AiExportCompactFields`, `Add-AiExportSection`, `ConvertTo-AiExportMarkdown`, `ConvertTo-AiExportJson`, and `Export-FileForAiAnalysis`, added right after `Show-ToolReportWindow` (same nested-function scope Sigcheck/Speakeasy's helpers already live in). Deliberately kept in lockstep with Winnow's module - same field list/order, same sentinel rules, same section layout - so an export of the same file looks the same regardless of which variant produced it. `New-ResultsPage`'s context menu gets a matching "Export for AI analysis (Markdown + JSON)" `ToolStripMenuItem` after the Speakeasy item, enabled/labeled via the existing `$launchMenu.Add_Opening` refresh handler based on `$Config.ReportDirectory` (no exe-path check, same reasoning as Winnow), record looked up via `$FileRecords.TryGetValue()` (a `ConcurrentDictionary`, so `TryGetValue` not a bare indexer - same pattern the Ghidra handler already uses). **No `pwsh` available in this dev sandbox to actually run the script** - verified the edit mechanically instead: extracted just the added lines from the diff and ran a quote/comment-aware brace/paren/bracket balance check against them in isolation (0 errors, 0 unclosed), confirmed all 3 insertion points sit at clean statement/function boundaries (never splitting an expression), and confirmed all 6 new function definitions and their call sites resolve by name via `grep`. This is the same verification discipline used for every other Rowan-only change in this project, since real syntax checking isn't possible here.
- [x] **Full Winnow test suite re-run after both changes** - 188 passed, the same 3 pre-existing failures as before this feature touched anything (`test_capa_scan.py`/`test_engine.py`, a capa rule-set version mismatch in this sandbox, unrelated to `ai_export.py` or `results.py`), confirming nothing else broke.

## Multi-vendor antivirus detection (both variants) - 2026-08-08

Raised directly: the Defender exclusion button only ever helps if Windows Defender is the machine's active antivirus product - on a machine running a different AV, that button does nothing useful and gives no indication why. This adds detection of whatever's actually registered, plus guidance for where to add the exclusion manually when it isn't Defender. Scope chosen deliberately narrow (detect + guide, not automate every vendor): most non-Defender products either have no stable local CLI for scripting an exclusion at all, or - for the enterprise EDR products especially (CrowdStrike, SentinelOne, Symantec/Broadcom managed via SEP Manager) - intentionally block local self-exclusion by design, since letting a local process silently exempt itself from EDR scanning is exactly the kind of thing EDR exists to prevent. Automating a scripted exclusion for those would be building something that either can't work or shouldn't.

- [x] **Winnow: `binsifter/core/av_detect.py` (new module).** Reads `root/SecurityCenter2`'s `AntiVirusProduct` WMI class - the same class Windows' own Security app reads, not a BinSifter invention - via a non-elevated `powershell.exe -Command "Get-CimInstance ... | ConvertTo-Json"` subprocess call, same shell-out pattern `defender.py` already uses (no `wmi`/`pywin32` dependency added). Unlike the actual Defender exclusion, reading this class needs no Administrator rights on any supported Windows version. `detect_av_products()` returns deduplicated `AvProduct` names; an empty list is a normal, valid outcome (Windows Server - the underlying Security Center service is client-SKU-only - Security Center disabled, or genuinely nothing registered), not an error - `AvDetectionError` is reserved for hard failures (non-Windows, `powershell.exe` missing, timeout). `guidance_for()` looks up a short, honest pointer to where ~17 common vendors' own exclusion settings live (McAfee, ESET, Sophos, Symantec/Broadcom, CrowdStrike, SentinelOne, Bitdefender, Kaspersky, Trend Micro, Malwarebytes, Avast/AVG, Webroot, F-Secure, Norton, plus both Defender name variants pointing back at the automated button), falling back to a generic "check its settings" message for anything unmatched. Also notes a real, non-obvious quirk worth remembering: Windows can suppress Defender's own SecurityCenter2 entry once a third-party real-time AV registers as active (Defender drops to passive mode but keeps running) - "Defender not in the list" doesn't necessarily mean Defender isn't present, only that something else is the active real-time scanner. Covered by `tests/test_av_detect.py` (14 tests - JSON bare-string-vs-array parsing since `ConvertTo-Json` emits a bare string for exactly one result, dedup, empty/blank/unparseable-output handling, the non-Windows/missing-powershell/timeout error paths, and the guidance lookup table), `subprocess.run`/`platform.system` mocked throughout since this dev sandbox has no real Windows/WMI to query.
- [x] **Winnow: wired into the Settings page.** New "Antivirus" section (`settings.py`) above the existing Defender section: a "Detect installed antivirus" button runs the detection and displays the result - detected product name(s) plus per-product guidance text, or a plain "none found" message distinguishing the Windows-Server case from genuinely nothing installed, or the error text if detection itself failed. No confirmation dialog needed (read-only, no elevation) unlike the Defender button beneath it.
- [x] **Rowan: ported field-for-field**, plus **a real pre-existing scoping bug found and fixed along the way.** While placing the new `Get-InstalledAvProducts`/`Get-AvGuidance` functions so the Settings page's click handler could actually reach them, re-checked how the existing `Add-DefenderExclusionPath` function (added earlier this same day) was wired - and found it was defined at the script's **top level**, outside `Show-MainWindow`, in the same scope as `Test-SystemDarkMode`/`New-STARunspace` (which only ever run in the script's own default/bootstrap runspace). But `Add-DefenderExclusionPath` is *called* from deep inside `Show-MainWindow`'s `$ps.AddScript({...})` block, which executes on a **separate runspace** (`New-STARunspace`'s `RunspaceFactory.CreateRunspace()` with a fresh `InitialSessionState`) that does not share a function table with the runspace that defined it. A function defined at top level is simply invisible from inside that separate runspace - calling it would fail at runtime with "the term ... is not recognized," the same as if the function didn't exist. Every other helper the Settings/Results pages actually call (`Invoke-CapturedTool`, `Show-ToolReportWindow`, the AI-export functions added earlier today) was already correctly defined *inside* that nested scope; `Add-DefenderExclusionPath` was the one exception, almost certainly written before this scoping distinction was front-of-mind. **Fixed by moving it into the nested scope** (right before `Invoke-CapturedTool`, alongside the new AV-detection functions) rather than leaving it as a live risk for tomorrow's real Defender-exclusion test. Not confirmed failing on a real machine (no `pwsh` in this dev sandbox to prove it either way) - the scoping violation itself is unambiguous from how PowerShell runspaces work, so this was fixed defensively rather than left as a question mark. New-ResultsPage-adjacent `New-SettingsPage` gets a matching "Detect installed antivirus" button/status label ahead of the Defender section, wired the same way. **No `pwsh` available in this dev sandbox** - verified mechanically instead: isolated the diff's added AND removed lines and ran a quote/comment-aware brace/paren/bracket balance check on each side independently (both fully balanced, 0 errors), then confirmed the net bracket-count delta between added and removed lines is symmetric for every bracket type (so the change can't have shifted the file's overall balance either), and confirmed every new function/control name resolves by name via `grep` at both its definition and every call site.
- [x] **Full Winnow test suite re-run after this feature too** - 202 passed (188 + 14 new `av_detect` tests), the same 3 pre-existing unrelated failures, confirming nothing broke.

## Rowan startup: $BinSifterRoot defaulting to C:\Windows\System32

- [x] **Fixed 2026-08-08.** Hit while testing the archive feature: launched Rowan and got two stacked warning dialogs - "couldn't determine its own script folder... will be created under the current folder instead: `C:\Windows\System32`", immediately followed by "default folder isn't writable... will be created here instead: `C:\Users\Jay\AppData\Local\BinSifter`". The second dialog's fallback (a per-user, guaranteed-writable folder) already made this harmless in practice, but it was correctly flagged that System32 should never have been the *candidate* default in the first place - "The default should be the GitHub directory or whichever directory Rowan is launched from."
  - **Root cause:** `$PSScriptRoot`/`$PSCommandPath`/`$MyInvocation.MyCommand.Path` all came back empty for this launch (same class of gap the code already called out - certain VS Code "Run and Debug" configs, or any invocation that doesn't leave a real, plain `.ps1`-file-backed script context, don't populate these the normal way). The old fallback for that case was `(Get-Location).Path` - PowerShell's own session *current directory*, which is a completely different thing from "where the script file lives" or "where Rowan was launched from," and for several real launch paths (a shortcut with no "Start in" set, some debug configs) that current directory defaults to `C:\Windows\System32` since that's where `powershell.exe` itself lives - not because BinSifter's own folder is anywhere near there.
  - **Fix:** added a fourth resolution tier, tried *before* giving up and guessing: parse this process's own command line (`Get-CimInstance Win32_Process -Filter "ProcessId=$PID"`) for a real `.ps1` path. This works regardless of which automatic variable failed, since the actual script path is always present as a literal command-line argument no matter how PowerShell was invoked (double-click, right-click "Run with PowerShell", a shortcut, VS Code Run/Debug, `pwsh -File`, `-Command "& '...'"`) - genuinely finds "whichever directory Rowan is launched from" as originally requested, rather than guessing. Only if that also comes up empty (e.g. the script's text was piped/typed into a session with no real file backing it at all) does it fall through to a last resort - and that last resort is now `%LOCALAPPDATA%\BinSifter` directly, not `Get-Location`, so System32 (or any other surprising session-CWD artifact) can never again show up as a candidate root, not even transiently in a dialog. The existing writability belt-and-suspenders check (added 2026-08-03 for this exact System32 case, previously the only defense) stays in place unchanged as a second layer, for the separate scenario of a *correctly*-resolved script folder that happens to live somewhere the current user can't write to (a read-only mount, a locked-down machine, etc.).
  - **Verification caveat, same as the rest of Rowan's port this cycle:** no interactive Windows/WinForms environment in this dev sandbox to actually run this and confirm the CIM-based lookup resolves correctly end-to-end - verified via careful manual read-through and the mechanical brace/bracket/paren balance check (balanced: 1185/1185 braces, 1047/1047 brackets, parens at the same pre-existing -1 baseline as before this edit). Worth relaunching however this was originally triggered (sounds like it wasn't a plain double-click or `pwsh -File`, since those already worked correctly before this fix) to confirm it now resolves to the real BinSifter/GitHub folder instead of prompting at all.

## Catalog-based (.cat) signature verification

- [x] **Implemented in Winnow 2026-08-08 - Rowan needed zero code changes.** Added directly after the "38 unparseable files" investigation surfaced the separate, longer-standing gap noted in `authenticode.py`: a large fraction of Windows' own inbox binaries (notepad.exe, calc.exe, etc.) aren't embedded-signed at all - they're validated against a system catalog file (`.cat`, under `C:\Windows\System32\CatRoot\`) instead, and `check_signature()` was previously only checking each file's own embedded PE certificate table.
  - **Rowan: confirmed to need no changes at all.** Rowan calls the native `Get-AuthenticodeSignature` cmdlet, which goes through Windows' own `WinVerifyTrust` under the hood - and `WinVerifyTrust` already checks catalog files transparently as part of normal signature verification on any real Windows machine with an intact `CatRoot`. This gap only ever existed in Winnow, which does its own PKCS#7/trust-chain verification via `signify` instead of deferring to the OS. Per the "implement across all variants" convention (see Conventions section at top), this is the one legitimate exception: there's nothing to port because Rowan was never missing the behavior in the first place.
  - **Winnow implementation:**
    - `core/config.py`: new `CatalogDirectory` Settings-page field (default `""`), added to `_CACHE_FIELDS`. Optional, same as `GhidraDir` - blank means "skip catalog checks," not an error.
    - `core/authenticode.py`: new `parse_catalogs(catalog_directory)` - enumerates every `*.cat` file directly under the directory and parses each into a `CertificateTrustList` via `CertificateTrustList.from_envelope()`; a single malformed `.cat` is logged and skipped, not fatal to the batch. `check_signature()` gained an optional `catalogs` parameter - each provided catalog is offered to the file via `AuthenticodeFile.add_catalog(catalog, check=True)` before `explain_verify()` runs; `check=True` means signify itself only actually attaches a catalog if the file's hash is found inside it (`find_subject()`), so passing every configured catalog to every file is safe and cheap. `iter_signatures()`'s existing default `signature_types="all"` already combines embedded + catalog signatures, so no other verification-path changes were needed.
    - `core/engine.py`: catalogs are parsed **once per worker process**, not once per file - `_pool_worker_init()` calls `authenticode.parse_catalogs(config.CatalogDirectory)` at worker startup and stores the result in a new `_worker_catalogs` global, mirroring the existing YARA-rules-compiled-once-per-worker pattern (a parsed `CertificateTrustList` wraps `cryptography` objects that aren't cleanly picklable across the `multiprocessing.Pool` process boundary, the same reason YARA's compiled `Rules` object is re-compiled per worker rather than handed down from the parent). The per-file worker call is now `authenticode.check_signature(path, catalogs=_worker_catalogs)`.
    - `gui/pages/settings.py` / `gui/settings_validation.py`: new "Catalog (.cat) directory - optional" Settings field, folder picker, validated the same way as `GhidraDir` (blank is valid; if given, must be a real existing directory) - factored into a shared `_OPTIONAL_DIR_FIELDS` loop rather than a second hand-copied `GhidraDir`-shaped `if`/`elif` block.
  - **Tests:** `tests/test_authenticode.py` gained coverage for `parse_catalogs()` (blank/missing/empty/non-`.cat`/malformed-`.cat`/file-instead-of-directory inputs, all degrading gracefully to an empty list rather than raising) and `check_signature()`'s new `catalogs` wiring (every provided catalog gets offered to `add_catalog()`; one catalog raising doesn't sink the others or the overall check; omitting the parameter entirely is unchanged from pre-2026-08-08 behavior) using a lightweight fake `AuthenticodeFile` stand-in rather than real crypto. Full suite: 173 tests, 170 passed - same 3 pre-existing/unrelated failures as before this work (confirmed via `git show HEAD:...` comparison: 2 in `test_engine.py` around capa-rules-corpus/progress-status gaps in the dev sandbox, 1 in `test_capa_scan.py` - none touch authenticode/catalog code, none introduced by this change).
  - **What's NOT done yet - a real `.cat` file was never obtained, so this is unit-tested plumbing, not end-to-end-verified against a genuine catalog.** The original ask (2026-08-07) was to "pull a known good cat and place it temporarily in the project directory" for testing. That could not be completed from this dev sandbox: there's no Windows machine here to pull one from directly, and the most likely alternative source - `signify`'s own library test fixtures (`oem89.cat`, `catfiletest.cat`, etc., referenced in its `tests/authenticode/file_types/test_ctl.py`) - turned out to be excluded from the PyPI sdist entirely (`tests/test_data/` isn't packaged), and fetching them from GitHub directly was blocked at every turn: `raw.githubusercontent.com`, `api.github.com`, and `codeload.github.com` all return `403 blocked-by-allowlist` from this sandbox's network policy (only the plain `github.com` web UI is reachable, which redirects tarball downloads straight into the blocked `codeload.github.com`). **What's ready to go:** a `Catalogs/` folder now exists at the repo root, gitignored (see `.gitignore` and `Catalogs/README.txt`) exactly as asked - drop a real `.cat` there (e.g. copied from a real machine's `C:\Windows\System32\CatRoot\{GUID}\`, which counts as "known good" since that Windows install already trusts it) and point Settings' new "Catalog Directory" field at it to exercise this for real. Worth a quick real-scan spot-check once that's in place, the same way the archive feature and the trust-store fix both got a real-scan confirmation before being marked fully done.
